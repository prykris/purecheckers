# Roadmap requirement audit

Decision update, 11 September: all six outstanding policy/operations questions are resolved in [owner decisions](owner-decisions.md). Invitations and unified themes are implemented locally; GitHub Issues is selected, open-source licensing is rejected, and the publisher/budget choices are recorded. Earlier pending-owner notes below are historical and must not trigger repeat questions.

Current audit: 11 September 2026. **The full goal is not complete.** This record maps requirements to current evidence instead of interpreting historical checkpoint prose as a completed checklist. A local implementation check does not close a requirement that also names a deployed URL, third-party tool, device or owner decision.

This record covers every numbered implementation item in [plan 01](01-technical-seo-fixes.md), its verification checklist and its remaining release dependencies. The plan 02 section maps the pipeline and current twelve-guide evidence, with explicit gaps still open. Plans 03–06, the 04a ruling and the broader player-experience matrix still require equivalent current-state audits. Their existing implementation work is preserved; omission from this pass is not a deferral or an absence claim.

## Evidence from this pass

- Read the current plan, route/load/layout sources, shared discovery policy, sitemap implementation, metadata components and relevant tests. Inspected the actual static share image and its Poppins-based generator. `git ls-files build site` returned no tracked files; these prior staged removals were preserved, not committed.
- The current production build passed (`.generated/build-seo-audit-final.log`). A temporary loopback server mounted the real Express app and built Svelte handler against the isolated test database. It ran no migrations, seed, fixtures, gameplay/socket startup jobs or production requests, and closed its server and Prisma connection after the checks.
- **112 checks passed across 17 local HTTP responses.** The audit parsed the actual HTML with Python's standard HTML parser, parsed JSON-LD with the JSON parser, parsed the sitemap with the XML parser and read PNG dimensions from their headers. Results and response artifacts are in `.generated/seo-http-audit/audit.json` and `.generated/seo-http-audit/responses.json`; the summary is `.generated/audit-seo-html-final.log`. These are local verification artifacts, not a published release.
- **31 focused tests across four files passed** (`.generated/tests-seo-audit.log`): `seoHead`, `sitemap`, `leaderboard` and `gameResults`. Source inspection established which assertions those tests actually cover. They do not prove all crawler or browser behavior.
- Initial audit failures are retained in `.generated/seo-audit-initial-findings.json`. Dynamic-page failures came from the audit server omitting adapter-node's explicit loopback HTTP origin. Navigation-count failures came from expecting a closed, conditionally rendered mobile menu in initial HTML. The app disables SSR, so its hydrated tab title is a source/build check, not an initial-HTML claim. These verification assumptions were corrected without weakening the corresponding product requirements.
- Three plan-conformance edits were made: the error template and error page now include Leaderboard; the app layout now supplies its tab title; the public leaderboard title explicitly names Top Players by ELO. Actual HTTP verification checks the error link and leaderboard title. Hydrated app title/mobile navigation acceptance remains manual.

## Plan 01, phase 1 — head and crawl fixes

| ID | Requirement | Current evidence | Remaining proof |
| --- | --- | --- | --- |
| 01.1.1 | Shared JSON-LD emission; home, FAQ and profile use it; ten FAQ questions parse | `JsonLd.svelte` escapes `<`; `seoHead` renders and parses normal/hostile input. Built home/FAQ schemas parse, including ten FAQ entries. `PlayerPage.svelte` uses the same component; profile/result rendering is covered by `gameResults` | Deployed home/FAQ/profile and Rich Results Test |
| 01.1.2 | 1200×630 PNG under 300 KB, specified artwork, safe text and OG alt/type | Actual PNG inspected: starting board, Poppins wordmark/tagline, dark palette and text inside the safe area. HTTP returns PNG, 1200×630, **58,754 bytes**. Built marketing heads contain the single image, alt/type and dimensions; `make-brand-images.py` uses the supplied font files | Deployed asset and real Facebook/LinkedIn/Discord previews; platform cropping |
| 01.1.3 | Canonical `/es`, slash redirect and consistent links | HTTP `/es/` returns 308 to `/es`; `/es` canonical/OG URL source is slash-free. Source URL search finds no trailing-slash Spanish links. The remaining `/es/` string is the hook's intentional language-prefix matcher, also specified by the plan | Deployed redirect/canonical and Search Console recognition |
| 01.1.4 | Home-only reciprocal hreflang and locale metadata | Parsed `/` and `/es` each carry exactly en/es/x-default and one correct locale/alternate. FAQ, strategy, leaderboard, games and changelog carry no hreflang | Deployed reciprocal tags and crawler inspection |
| 01.1.5 | Crawl public replays/invites and assets; block private app/API routes | `robots.txt` has anchored `/game$`, `Allow: /game/`, API/invite and listed app rules, with no `/auth`, `/join` or `/_app` block. `seoHead` checks these dependencies. The actual file is served locally | Search Console fetch/tester behavior for public and app URLs; query-bearing app URLs also need the plan 06 dependency review |
| 01.1.6 | One description/theme color, zoom-enabled viewport and language placeholder | `app.html` contains the viewport and `%lang%`, with no duplicate description/theme color. Parsed marketing responses have one description/theme-color and `width=device-width, initial-scale=1` | Deployed HTML and Lighthouse's viewport audit |
| 01.1.7 | Hook language/noindex, plus app tab title | `hooks.server.js` applies language and app header; built `/es` is Spanish; local `/auth` returns `X-Robots-Tag: noindex`. The app layout now declares `Pure Checkers` in its head; the app remains intentionally client-rendered | Hydrated tab title and deployed app header |
| 01.1.8 | Temporarily noindex empty blog/strategy stubs | **Superseded by shipped local content:** `/blog` is a real 301 to `/strategy`; the built strategy hub contains the twelve-article catalogue and is indexable. Sitemap includes the hub/articles and excludes `/blog`. Restoring the temporary noindex would contradict plan 02 | Deployed redirect, hub/articles and URL Inspection; do not expect the old stub-only verification command to return noindex |
| 01.1.9 | Encoded player URLs and guest/bot noindex | Player loader/component encode usernames and derive indexability from account flags. Replay participant URLs come from the shared public API projection. `leaderboard` checks encoded discovery and flag-based eligibility, including a human whose name starts with Bot. Private/tombstoned profiles are hidden according to current policy | Deployed encoded profile/replay URLs and bot/guest metadata; positive/negative profile head cases were source-inspected, not newly exercised over HTTP in this pass |
| 01.1.10 | All ten FAQ answers in SSR HTML | Parsed `/faq` contains ten answer elements and ten JSON-LD question entries without running client JavaScript | Deployed HTML / crawler fetch |
| 01.1.11 | One descriptive English/Spanish home H1 | Both HTTP responses have exactly one non-brand H1 with the planned play/free proposition. Brand marks remain paragraphs | Deployed/rendered mobile acceptance |
| 01.1.12 | Remove tracked build/site; add 192/512 PNG manifest icons | Git index lists no `build` or `site` paths. Manifest references both PNG sizes; actual HTTP images have matching dimensions. The image generator shares favicon-style artwork | Commit/repository publication and actual PWA/device icon rendering |

## Plan 01, phase 2 — dynamic sitemap

| ID | Requirement | Current evidence | Remaining proof |
| --- | --- | --- | --- |
| 01.2.1 | Express sitemap route, API mounting and Vite proxy | `server/routes/sitemap.js` is mounted before app routes/static handling; Vite proxies root and shard paths. The local built-app server returns the generated XML | Deployed route precedence and development proxy smoke check on the final candidate |
| 01.2.2 | Static, eligible player and eligible game groups; dates/encoding; home alternates | `publicGames.js` owns actual guest/bot flags, non-aborted and ten-move rules for API/list/sitemap. `leaderboard` uses real DB fixtures for inclusions/exclusions. XML parsing confirms static discovery, twelve article URLs, published puzzle entries and home-only alternates. Static `lastmod` remains the explicit release constant; no fake deploy date was stamped | Review static lastmod at release, then inspect deployed entries/dates as real content accumulates |
| 01.2.3 | Ten-minute public cache, coalescing and 40,000-URL splitting | Source defaults are 600,000 ms and 40,000 URLs. `sitemap` tests expiry/coalescing, shard splitting/bounds and recovery after failure. Parsed HTTP header has bounded public max-age. Publication caching also expires at UTC midnight | Production capacity/cache behavior and a real indexed sitemap submission; small-shard tests verify the same splitting path, not a 40,000-row production load |
| 01.2.4 | Remove static sitemap while preserving robots reference | `src/static/sitemap.xml` is absent; robots points to the dynamic root and HTTP serves it | Published repository/deployed static artifacts |
| 01.2.5 | Test eligible/hidden rows and valid XML; submit sitemap | `leaderboard` exercises real registered/guest/bot/aborted/short-game policy against API and sitemap. `sitemap` covers XML escaping, structure and shards; this pass additionally parses the actual HTTP XML with an XML parser | Search Console resubmission, success status and discovered URL count. No submission was made |

## Plan 01, phase 3 — discovery and page schema

| ID | Requirement | Current evidence | Remaining proof |
| --- | --- | --- | --- |
| 01.3.1 | SSR leaderboard, explicit ELO title, rankings table/empty state | Loader reads the public API; `PublicIndex` supplies metadata and one H1. Local HTTP is 200 with the planned ELO title. Source supplies rank/player/ELO/W/L/games and relaunch empty-state CTA; API eligibility is covered by `leaderboard` | Deployed populated/empty views and narrow-screen table scrolling |
| 01.3.2 | SSR recent games, shared `public=1` filter and replay/profile links | Local HTTP is 200 with server-rendered content. Loader/API use `public=1`; `publicGames.js` and `leaderboard` establish shared eligibility. Private profiles intentionally have no link, consistent with the disclosed privacy policy | Deployed populated list and navigation |
| 01.3.3 | Sidebar/mobile/footer discovery links, home feature link and sitemap entries | Actual SSR marketing responses expose sidebar/footer links; source includes the conditionally rendered mobile-menu links and home leaderboard feature link. Parsed sitemap contains both discovery routes | Hydrated mobile-menu interaction and deployed navigation |
| 01.3.4 | Shared replay indexability, H1 and SSR numbered move list | Replay loader consumes API `game.indexable`; component renders noindex when false, a descriptive H1 and the move list using `shared/gameHistory.js` and `shared/notation.js`. Live history shares this engine-derived projection; replay controls stop at an invalid step with a visible explanation. `gameHistory` tests cover chains, crowns and invalid records. `leaderboard` tests thresholds/identity flags; `gameResults` renders actual replay components and results | Deployed eligible/ineligible replay HTML, full numbered list and crawler inspection |
| 01.3.5 | Player/replay BreadcrumbList and visible breadcrumbs | Both current page components use shared JsonLd and visible navigation. `gameResults` server-renders both; JsonLd escaping is independently exercised | Specific schema validation in Rich Results Test on deployed player/replay pages |
| 01.3.6 | Linked WebSite/Organization graph, SearchAction and home WebApplication | Marketing layout supplies WebSite/Organization with linked IDs, player SearchAction, empty sameAs and the square logo; home supplies WebApplication. Parsed HTTP schemas confirm actual JSON emission across marketing pages. No social identity was invented | Schema Markup Validator on deployed graph; semantic schema conformance is not established merely by JSON parsing |
| 01.3.7 | Styled fallback/error page with Home/Leaderboard/FAQ links | Both error sources contain the planned links after this pass. Local unknown-path HTTP is 404, with the helpful links parsed from the actual Svelte error page. Static fallback template was source-inspected | Deployed 404 and controlled fatal-error fallback rendering; this pass did not induce a production/SSR fatal error |

## Explicit verification and outcome gates

The plan's curl checklist is covered locally for asset status/type, Spanish redirect/canonical/language, FAQ description/hreflang/questions, home alternates, auth noindex, strategy's **current** indexability, sitemap parsing/content and robots delivery. The bot-profile head check is supported by source/API tests but was not added to this pass's read-only HTTP fixture. Each command must still be run against the exact deployed candidate. A local canonical pointing to the production origin is expected and does not prove that production serves it.

The following named external checks remain **unverified**: Search Console robots refresh/tester; URL Inspection for home/Spanish/FAQ/strategy/registered profile, including rendered screenshot and declared canonical; indexing requests for home/Spanish; sitemap replacement/submission and discovered counts; Rich Results Test for home/FAQ/player/replay; Schema Markup Validator for site/organization/application graph; real Facebook, LinkedIn, Discord, Slack and X/composer previews; mobile Lighthouse viewport and SEO score. No community message, preview-post submission or Search Console mutation was performed. Current platform-tool availability must be rechecked when executing those steps.

The plan's 30/90-day traffic/indexing metrics are measurement targets. No observed growth, indexing, social-card cache miss, production performance or deployment success is inferred from these checks. Candidate commit, migration review, target-host build and deployment identity remain governed by [launch-readiness.md](launch-readiness.md).

## Remaining audit queue — original scope preserved

| Scope | Next evidence needed |
| --- | --- |
| [Plan 02](02-strategy-content-hub.md) | Close the exact conformance gaps and manual/external checks listed below; the pipeline and rendered article checks are mapped, not a complete editorial or release acceptance |
| [Plan 03](03-daily-puzzle.md) | Recovery/API/controller evidence is mapped below. Complete generator/commentary and rendered-page conformance, then verify actual production buffer/runner separately from the checked-in artifact |
| [Plan 04](04-first-session-empty-server.md), [04a ruling](04a-lobby-design-debate.md) and [brief](04a-lobby-design-brief.md) | Apply explicit supersessions, then trace all first-session/nearby/search/result/guest/reward/analytics journeys and their manual acceptance evidence |
| [Plan 05](05-backlinks-distribution.md) | Map venue research, exact draft/artifact requirements, launch gates and owner-authorized outreach/measurement; planned text is not a sent submission |
| [Plan 06](06-share-loop.md) | Complete the open rows in the [sharing requirement map](../share-previews.md#requirement-map-and-remaining-proof): targeted challenge, prompt/browser and production/platform checks. Local rate admission, result-field projections and version-3 centered card geometry now have explicit evidence |
| Overview / reliability | Revalidate each player-experience finding and transition row, bot worker requirements and remaining resource/appearance behavior; complete theme policy/application after the owner decision |

Pending owner/external requirements remain: theme policy, licence and feedback destination, puzzle runner/production publication and monitoring, target capacity/Linux/Railway evidence, candidate release, broader browser acceptance and any separately authorized outreach. This queue is not a replacement or reduction of the active goal.

## Plan 06 — sharing contract audit, 11 September 2026

Prompt/disclosure follow-up: **37 focused tests across three files** pass, including controlled grant lifetimes, stale-readable/failed-write storage, account/privacy changes and the puzzle reward transaction using its now-shared constant. The component delegates to one prompt owner. The production build and parsed eleven-question FAQ HTML/JSON-LD confirm the new guest/profile/cache disclosure and current reward wording. [Prompt lifecycle notes](../share-previews.md#prompt-lifecycle-and-public-disclosures) distinguish this from hydrated, simultaneous-tab and GA4 evidence still required.

Card follow-up: version 3 resolves the incompatible original left-board and centered-crop coordinates, adds the missing result/invite details and corrects wager/account copy. **51 focused tests across five files** pass; twelve full-size fixture cards and square crops were rendered and inspected. Actual font/content bounds, minimum-size ellipses, full maximum wager amounts and retained outcomes are verified. The [version-3 notes](../share-previews.md#version-3-content-fitting-and-centered-crops) state the explicit layout supersession and local timing limits. Actual platform crops/caches and target-host measurements remain open.

Admission/projection follow-up: **33 focused tests across four files** pass (`.generated/tests-preview-admission-fields.log`). Actual-router tests cover both preview budgets and early rejection; real database/API evidence follows nonzero rating changes and endedAt across replay, both lists and both participant profiles. These close the previously missing local proof for phase 0.5 and admission in phase 2.3. Production performance/proxy behavior remains open. The owner question about open-room versus pending challenges is awaiting a response.

The [requirement map](../share-previews.md#requirement-map-and-remaining-proof) now covers phases 0–4, the profile challenge surface and the named external completion gates. This pass fixes result/profile text conformance, attempt/cancellation metadata, unusual native failures, the query-bearing robots dependency, and stale advertised preview versions. One shared constant now serves renderer cache identity and all four actual page loaders; replay metadata includes article type and its result.

**45 focused tests across seven files** passed (`.generated/tests-share-contract.log`). Tests include personal/neutral messages, long names, puzzle spoilers, fallback/cancellation ordering, actual page loaders, API-backed SSR result views, real worker PNG dimensions, privacy/expiry revalidation and cache recovery. This is scoped implementation evidence. The profile Challenge CTA still only opens sign-in; image geometry/crops, exact rate admission, complete list/profile field mapping, prompt/browser behavior and all deployed measurements remain open.

## Plan 02 — pipeline and rendered article audit

Current source inspection covered the plan, mdsvex configuration, content index and manifest generator, article/hub routes, ArticleLayout, DiagramBoard, browser search adapter and diagram test assertions. A fresh production build passed (`.generated/build-strategy-audit-final.log`). **22 focused tests across three files passed** (`.generated/tests-strategy-audit.log`): strategy diagrams, board keyboard navigation and SEO head behavior. The diagram verifier now rejects a starting side with no legal move, including a blocked-men regression case. It independently validates **29 diagrams in twelve article sources** (`.generated/audit-strategy-source-final.log`).

Python's HTML parser inspected the actual twelve prerendered pages and parsed their JSON-LD: **316 checks passed** (`.generated/strategy-rendered-audit.json`, `.generated/audit-strategy-rendered-final.log`). These cover metadata multiplicity/values, title and description lengths, canonical/OG URLs and dates, Article/BreadcrumbList, visible FAQ questions matching schema, heading levels, contextual link destinations, required FAQ/home links, two shell Play links, three related cards, SSR board cells/accessibility labels/captions/piece lists and the three screenshot figures. They do not establish browser interaction, editorial accuracy, indexing or schema-validator approval.

Initial findings included eight guides missing contextual FAQ links and two rules guides missing the quick-setup link; their introductions now contain relevant links and their updated dates reflect the edit. The hub now describes the completed tactics/endgame coverage, removes an unsupported weekly publication promise and uses the existing analytics helper. The initial audit also misread UTF-8 metadata using Windows' default encoding and treated the friends walkthrough's instructional Play link as an unwanted third CTA; those audit assumptions were corrected. No product metadata was changed to accommodate either fixture error.

| ID | Requirement | Current evidence / remaining work |
| --- | --- | --- |
| 02.1.1 | mdsvex configuration and successful prerender | `.md` extension and mdsvex preprocessing are configured; current build passes and emits all twelve article HTML files |
| 02.1.2 | Shared index, reading time, lookup, related and prev/next | `strategy.js` owns these functions. Category/date/slug ordering is deterministic; related first uses frontmatter, then category, then other articles to fill three cards. The sitemap consumes a build-generated metadata manifest, avoiding Svelte imports in the Node server |
| 02.1.3 | Validated frontmatter | `strategyCatalog.js` now validates both page and manifest builds: required strings/categories, real calendar dates and date order, unique/nonempty references, known related targets, FAQ count/entries/duplicates and safe image URL shapes. Direct contract tests cover rejection and valid leap dates. Unsupported translation metadata fails explicitly until translated routes exist |
| 02.1.4 | SSR static/step/try boards, markings, legal moves and bot replies | Actual HTML contains 64 cells, position labels and hidden piece lists. Scripted examples replay through the real engine. Browser search uses the existing bounded worker pool, abort signal and legal-result check; reset/disposal advance ownership generation. Interactive boards use `group` rather than `img` so controls remain accessible; static boards use `img`. Keyboard, reset-during-search and reduced-motion acceptance remain scoped to the existing manual checklist |
| 02.1.5 | Shared article shell, head/schema, CTA, FAQ, navigation and width | Shared layout supplies metadata, visible dates/author, 640px prose, FAQ/schema, three related cards, prev/next and closing CTA. The strategy-only mdsvex plugin inserts shared ArticlePlayLink after the intro, before the first H2. All twelve built pages pass exact placement checks; authors contain no copied CTA/tracking code |
| 02.1.6 | Explicit entries, article route and unknown-slug handling | Route declares prerender and `entries()`, loads by slug and throws 404 when absent; keyed article subtree remounts on slug change. Every current entry has actual built HTML; a real local built-app request for an unknown guide returns 404 |
| 02.1.7 | Hub intro, ordered categories/cards, CTA and schema | Hub uses the index/category projection, hides empty categories, includes reading time/updated dates and emits CollectionPage/BreadcrumbList. The actual intro passes the 150–250-word range; all twelve cards match catalogue order/title/description/reading time. Broader editorial wording checks remain separate |
| 02.1.8 | Blog redirect and removed discovery links | Plan 01's real HTTP check establishes 301 to the populated hub and sitemap exclusion; no stub resurrection |
| 02.1.9 | Home/FAQ links into guides | Existing routes expose guide links; current article audit checks the reverse FAQ and rules/home links. Exact FAQ-answer wording and all inbound anchor placement remain an editorial comparison, not proven by article link counts |
| 02.1.10 | Content and diagram tests | All current diagrams have legal starting moves and legal scripted continuations. Tactical proof tests check actual claims. `strategyCatalog.test.js` now validates the actual twelve-guide corpus and exercises invalid metadata, duplicate/unknown slugs, navigation boundaries/related fallback and intro compilation |

### Twelve-guide evidence

All titles are at most 60 characters and descriptions are 140–155 characters in actual rendered HTML. Every article has three to five FAQ entries, matching visible questions and schema. Source-specific factual review records and earlier browser evidence remain in [strategy-verification.md](../strategy-verification.md); this pass does not claim a new independent editorial review.

| Article | Current diagram / evidence |
| --- | --- |
| Mandatory jumping | Two diagrams; forced backward capture and blocked quiet moves have engine assertions |
| King movement | Four diagrams; static variant marks, flying destinations and captures checked against the engine |
| Rules pillar | Two step diagrams; setup and promotion, labelled house-rule differences and contextual links |
| First move | Two diagrams; source-hashed opening experiment verifies all seven choices and completed depth-six results |
| Backward movement | Two diagrams; standard/Pure illustrative distinction and legal practice capture |
| Checkers vs draughts | One engine board plus the existing 10×10 SVG; variant-source review remains in the verification record |
| Beginner openings | Five boards; tests check exchanges, optional recaptures, supporting moves and blocked landings |
| Double/triple jumps | Three boards; sacrifice guard, complete chain and promotion independently asserted |
| Traps | Five boards; all breeches replies, alternative king landings, deflection cost and dog-hole outcomes checked. The corrected title avoids claiming every example wins material |
| Two kings vs one | Two boards; source-hashed winning certificate checks all 15 terminal defensive branches. This explicitly supersedes the outline's unsupported inevitable-draw premise |
| Playing the computer | One board; earlier reviewed engine evaluation/search explanation. No universal win against Easy is inferred from a single position |
| Friends walkthrough | Three actual annotated screenshot figures, with alt text/captions. No diagram is required by this article's specific brief |

### Remaining plan 02 acceptance

- Leading CTA placement, content-contract coverage, full rendered prev/next ordering, hub/card metadata and unknown-slug HTTP behavior are now verified locally. Evidence: 49 focused tests, passing build, 367 parsed rendered checks and five read-only built-app HTTP responses. See [strategy verification](../strategy-verification.md#catalogue-and-intro-placement--11-september-2026) for logs and scope. Device and deployed acceptance remain open.
- The later [rendered editorial check](../strategy-verification.md#rendered-editorial-checks--11-september-2026) closes the body-length and contextual-link findings: all twelve articles meet their planned ranges, the computer guide has 1,550 body words after adding a replay-based practice step, and every article has two to four contextual destinations. Counts exclude FAQ, figures/captions and the injected CTA.
- The rendered FAQ comparison now reads the actual eleven public FAQ buttons and answers, correcting the earlier empty-heading comparison. The 51 article FAQ questions are distinct across articles and from the public FAQ; no public FAQ sentence of eight or more words is reused in article paragraphs/answers. Literal primary-query/title/description alignment and comprehensive outline/source coverage remain open; the [editorial report](../strategy-verification.md#rendered-editorial-checks--11-september-2026) records those limits explicitly.
- Spanish publication is intentionally deferred in the plan. Nested Spanish imports, routes, translation pairing and reciprocal article/sitemap alternates are **not** implemented. The contract now rejects non-English `lang` and any `translationOf` instead of accepting metadata that cannot produce correct routes. Do not call those future capabilities implemented. Existing English pages correctly have no false alternates.
- Apply the existing phone, keyboard, reduced-motion, reset-during-bot-search and navigation checks. Verify analytics in GA4 DebugView, deployed article URLs, external schema tools and Search Console separately. Publication dates in source and passing local builds do not prove production release or indexing. Monthly refresh/indexing/traffic targets remain future measurements, not observed outcomes.

## Plan 03 — recovery, API and controller pass

The current plan, puzzle API/service, Prisma models, progress journal/controller, date watcher, page loader/components and relevant test assertions were read. This pass found and fixed source-confirmed recovery failures rather than assuming that the older puzzle implementation checkpoint closed them.

- Shared progress validators reject malformed confirmations/history. The browser uses the existing request deadline and account authentication generation; errors expose Retry, storage failures are visible, and disposed owners cannot publish old completions. Confirmed server progress underlies pending local work. A reveal recorded in another tab supersedes a conflicting local solve without discarding newer pending counts. Retrying an already-earned reward refreshes the current profile through its existing owner.
- Puzzle writes now take the shared account lock and recheck retirement before the puzzle identity lock. The existing attempt/wallet/ledger/reward transaction remains the only reward mechanism; no new receipt table, migration or coin policy was introduced.
- A midnight page reload advances the browser's acknowledged day only on success. Focus/online events and a bounded retry timer recover failures; pending reloads coalesce and disposal removes their scheduling.
- **793 tests across 81 files passed** (`.generated/tests-puzzle-recovery-full.log`). The final **32 focused tests across four files passed** (`.generated/tests-puzzle-recovery-acceptance.log`), including a strengthened test that plays all 31 buffer puzzles through the actual browser controller before validating its submitted turns. The final production build passed (`.generated/build-puzzle-recovery-release.log`). See [puzzles.md](../puzzles.md#recovery-verification--11-september-2026) for exact scenarios and concise manual checks. None of this is production publication or a full plan 03 completion claim.

| ID | Requirement | Current evidence / remaining proof |
| --- | --- | --- |
| 03.1.1 | Complete-turn search, terminal scores, quiescence and bounded verification | Source tracing covers iterative deepening, terminal scoring, capture extension, history-free clones and conservative window bounds. An independent exhaustive reference matches depth-three search and root-choice bounds for all 31 launch positions. Regression cases cover the stronger non-first chain branch, immobilisation, horizon extension, two winning choices, budget exhaustion, a seeded shot and symmetry. Actual CLI re-verifies all 31 at recorded depths. Finite search remains bounded evidence, not unlimited proof |
| 03.1.2 | Puzzle/attempt models, reward reason and deletion-safe identity | Current schema has Puzzle, PuzzleAttempt, difficulty, reward reason, `revealed`, unique account/visitor attempt keys and SetNull user deletion. Real API tests exercise duplicate identity writes and deleting a guest while preserving its attempt; test migration deployment passes |
| 03.1.3 | Generator source/selection/verification/commentary/dedupe/calendar/CLI | Generator 1.2.3 shares discovery eligibility/keyset scanning and whole-replay validation with API attribution. Source tracing covers prescan/selection thresholds, all-decision verification, best-defence checks, realized endpoints, classification, symmetry/calendar reuse and CLI bounds. Import now validates structure, exact engine annotations/classification and depth at least max(line length + 2, 8). 61 focused tests and actual 31-row CLI validation pass. All 31 explanations and six theme paragraphs now have a [documented editorial review](../puzzle-editorial-review.md), including individual tactical notes and legal sample lines. Production operations remain open. Durable rejection evidence now blocks later generation and imports, including mirrored positions. Nightly mutation evidence is recorded below. Off-rotation substitutions are explicitly logged and are a documented exception to the ideal weekly mix |
| 03.1.4 | Reviewed and uniquely verified production buffer | Local artifact has 31 distinct dates/positions; every line passes controller playthrough and fresh actual import/deep verification under the stricter definition checks. **Production import, all-position owner acceptance and actual host coverage remain open** |
| 03.2.1 | Published API, date filtering, attempts and deletion behavior | Actual API tests cover today/past/future, malformed dates, cache headers, anonymous/account duplicate attempts, guest deletion, proof rejection, reveal disqualification and guest upgrade. Added real retirement-lock contention and ledger-failure rollback tests. Current publication navigation and aggregate-stat query were source-inspected |
| 03.2.2 | SSR dated pages, metadata/schema, breadcrumbs, navigation and explanations | These source-confirmed gaps are now closed locally: date/side titles, difficulty/theme/date/side descriptions, WebPage publication/reveal dates, a shared visible/schema three-item breadcrumb and published head prev/next links. Actual built-app HTTP checks verify current/archive/empty/future states, metadata multiplicity, board cells and spoiler boundaries. Archived fixture explanation has 330 visible words. External schema/device acceptance remains open; the later [editorial review](../puzzle-editorial-review.md) records individual notes and a non-repeated-sentence count for every local entry |
| 03.3.1 | Select/drag/keyboard, complete chains, hint/reveal and motion | Current board funnels pointer and keyboard moves to PuzzleController; controller tests cover wrong-turn reset, hint/reveal, cancellation and account replacement. Server rejection now also corrects local solved feedback. Actual phone/drag/reduced-motion acceptance remains open |
| 03.3.2 | Every buffer puzzle completes through the interactive validator | **Now directly verified:** all 31 launch puzzles play through PuzzleController, including automatic opponent replies and complete player chains, reach solved state and produce server-valid solve proofs |
| 03.4.1 | UTC history, upload, rewards, sharing and analytics | Journal tests cover one-time visitor claim, account/authentication isolation, offline proof retention, timeout/retry, malformed responses, authoritative reveals and failed history reads. Date tests cover failed midnight reloads, coalescing, suspended tabs and disposal. API tests prove one eligible coin under concurrent retries and no archive/guest mint. ShareActions/tracking remain current source evidence; exact share text, native/clipboard flows and GA4 need the sharing/browser audit |
| 03.5.1 | Discovery links and homepage puzzle teaser | Source has mobile/sidebar/footer puzzle links; actual home/FAQ/guide HTML contains the sidebar/footer links, and the home teaser contains 64 SVG squares and follows How to Play. ArticleLayout supplies the article puzzle link. Theme labels now link to the correct existing guide using a shared presentation mapping. Hydrated mobile-menu/device acceptance remains open |
| 03.5.2 | Published sitemap dates and per-puzzle images | Actual HTTP XML includes current/past dates with correct reveal lastmod and excludes the future date. Page OG URLs point to their dated cards; both generated fixture PNGs return 200 at 1200×630, while the future preview remains generic. Current card visually inspected without spoilers. Deployed URL/platform-cache acceptance remains open |
| 03.5.3 | Consistent house-rule explanations | Current FAQ and home source describe compulsory captures, backward man captures, flying kings and promotion ending the capture turn. The puzzle hub now explicitly includes that promotion rule, and puzzle pages link the rules guide. The built hub has 205 evergreen-section words. Broader editorial/variant source review remains scoped to the existing article verification |

Page-pass evidence: **22 focused tests**, final production build and **106 parsed checks across 16 actual local HTTP responses** passed. [Puzzle page metadata and discovery](../puzzles.md#page-metadata-and-discovery--11-september-2026) records the exact fixture safeguards, logs and remaining external checks. Titles deliberately avoid the outline's universal “and win” wording: some verified combinations gain material rather than end the game.

Nightly mutation coverage now includes pure conclusive/inconclusive preparation, depth caps, PostgreSQL preservation of a row published since preparation, stale evidence, retained attempts, competing proposals, same-run rejection exclusion and replacement rollback. Search work happens outside the bounded publishing transaction; the commit uses the database clock and a publication protection window. **39 focused tests passed**, and the production build passed. Details and final full-suite evidence are in [puzzle operations](../puzzle-operations.md#local-verification).

Durable-rejection follow-up: migration `20260911130000_puzzle_rejections` preserves conclusive evidence independently of deleted puzzle rows. Generation/import/commit share the exclusion set; stale evidence and budget exhaustion do not create rejections. Status reports rejected dates and keeps exit 5 for retained failures across later runs. **838 tests across 86 files** and the production build pass; production migration remains pending.

Additional explicit work remains: per-page unique commentary and shared theme prose, actual-device acceptance, verified production publication/monitoring and the feedback destination. Source attribution and the selection/import audit now have the evidence mapped above. The original proposal to shift unpublished dates is intentionally superseded by stable URLs and hole filling in the operations checkpoint; do not restore re-dating. The accepted numbered square notation is shared with the rest of the app. The owner reward approval remains one coin for the first eligible solve of today's UTC puzzle, never archive coins. Production/runtime measurements and growth metrics are not inferred from tests.

## Plans 04 and 04a: entry audit started, 11 September 2026

Analytics follow-up: game lifecycle events now follow accepted snapshots rather than mounts and acknowledgements. A participant/game journal suppresses duplicates and historical recovery events; confirmed journeys replace global pre-command source markers. **31 focused tests across two files** include SessionClient loss/recovery integration. [First-session verification](../first-session-verification.md) defines the narrower source taxonomy and remaining limits: `first_session` cohort reporting, complete invite conversion attribution and actual GA4 delivery are still open. This is not completion of plan 04's measurement requirements.

Waiting-room follow-up: the UI now consumes server-projected effective readiness and room-level action availability instead of deriving a failure from WAITING or offering bots in wager rooms. Start eligibility and presentation use the same readiness predicate; authorization remains in the command owner. A scoped ScreenWakeLock replaces the unowned asynchronous requests. **93 focused tests** cover resource races and room/socket behavior; actual phone scan and power behavior remain manual. See [first-session verification](../first-session-verification.md) for evidence and remaining scope.

The lobby follow-up replaced the stale global bot roster with the existing scoped read owner and complete identity validation, retaining difficulty fallback and explicit Retry. It adds native radio controls, fixes denied-storage initialization, and ties automatic first-game Bot selection to the accepted game/hint transition without overriding explicit tab choices. **893 tests across 91 files** pass. The requirement table in [first-session verification](../first-session-verification.md) separates current source/test evidence from actual-browser, 360px, scan and full action-matrix acceptance.

The subsequent backend passes order human pairing, commands and reconnect restoration through shared participant queues and carry room-creation guards through the persistence transaction. Exact pool claims now isolate cancel/rejoin work, retain the unaffected searcher's wait/order and prevent duplicate pairing. Failed creation confirms the database outcome and restores existing membership before requeue; unavailable confirmation keeps later commands ordered, with interruptible retry backoff. Tests include competing bot/human admissions, replacement connections, transaction rollback, lost responses, projection/read failures and shutdown while queued/retrying. [First-session verification](../first-session-verification.md) records that evidence; the complete action matrix, actual-device flows, analytics and remaining policies are not proven by these tests.

The owner-approved 04a constraints preserve three lobby tabs and automatic scan entry; they supersede the conflicting merged-tab/manual-auth sections of 04. Current AuthScreen, AppBootstrap/layout, account owner/API, BotPlay/BotChips, PlayTabs, SearchScreen and analytics helper were inspected. The authentication issues found in this pass are fixed through a single form lifecycle owner, existing deadlines and account-generation checks. Automatic invite/profile requests now receive bounded signals; a late nickname cannot overwrite typing. Search copy reflects unmatched searchers honestly.

**60 focused tests across four files** pass. [First-session verification](../first-session-verification.md) records scenarios, manual checks and the remaining scope. This is entry/recovery evidence, not proof of the complete 04/04a action matrix, actual-device scan flow, backend transition/reward policy or analytics delivery. Those remain open for the next audit passes.


## Plan 04 — first-session measurement checkpoint

The success-metrics table explicitly requires `game_end` with `first_session = true`. This field now follows GA4 `session_number` via its documented API, with known/unknown coverage, a 250ms deadline and no independent visitor/session storage. Accepted game events still originate solely in GameAnalytics. **59 focused tests across four files** and the build pass; [first-session verification](../first-session-verification.md#ga4-first-session-cohort-and-source-correction) maps exact local coverage and required GA4 acceptance.

The audit also contradicted the former invitation attribution claim: every room-origin game was labelled invite. These now report room, including normalized old journals. True link-to-membership-to-game attribution remains unfinished, alongside property custom dimensions, production delivery, session-cohort funnel/retention reporting and actual targets. Local mock calls do not prove any target metric or production traffic conversion. This checkpoint does not close the complete plan 04/04a requirement matrix.

## Plan 04/04a — accepted invitation entry checkpoint

Invitation attribution now follows committed code-based player admission. Directory joins use IDs; the existing navigation controller preserves the code through authentication/recovery and sends it once after an accepted idle snapshot. The room records the entry fact in its membership transaction; game reservation captures invited humans on GameRun; restored participant snapshots expose only the viewer's boolean. The client observer uses that evidence for source invite and preserves confirmed source in its existing event journal. Hosts, directory entrants, rejected commands, spectators and rematches do not become new invite conversions. Older rooms/games remain compatible and unknown rather than inferred. See [durable-room contract](../durable-rooms.md#accepted-invitation-entry).

This trace also found and fixed an actual 04a path failure: invitation navigation sends a code to spectate a playing room, while the handler only resolved a room ID. Both command paths now support code lookup with the existing private-room checks. Local domain tests cover invitation-code spectator membership, and navigation tests cover dispatch after the playing-room precheck. Actual browser acceptance remains separate.

The additive `game_invitation_entry` migration has been applied only to the local test database. The first focused run passed 177 tests across seven files; the latest implementation-status checkpoint records final full-suite evidence, including rematch isolation and sign-in/navigation coverage. The first full run also exposed stale research hashes after the earlier shared puzzle-reward constant change. Both research artifacts were regenerated by their original scripts; all experimental values and proof branches match their predecessors exactly. Only generation time and the constants hash changed, so article claims remain supported by the same results.

The original invitation metric's `/join/*` denominator excludes the approved QR route `/invite/*`. Production reporting must include both and deduplicate within a GA session; source invite now denotes accepted code entry from either. Browser/GA4 delivery, reporting setup and conversion targets remain externally unverified, not completed by local server/client tests. The wider 04/04a matrix and full roadmap remain open.

## Plan 04 phase 4 / 04a — result and upgrade matrix

| Requirement | Current evidence | Remaining proof |
| --- | --- | --- |
| 04.4.1 Result actions, source-specific next game, real counts, rematch state and guest save | GameResult implements the later 04a origin/account matrix. Six actual SSR rows plus conditional search, consent/departure and pending/recovery cases pass. Play again now uses the accepted finished difficulty, independent of lobby preferences | Hydrated clicks and actual 360px result/sharing geometry |
| 04a One accepted command from a finished game | Existing dispatcher releases the finished result before idle commands; gameSocket tests check bot continuation and opponent release. Durable result tests cover rematch consent, transaction rollback, retry and expiry | Multi-device manual continuation, especially interrupted/slow requests |
| 04.4.2 Inline upgrade preserving identity and progress | Layout-level UpgradeSheet now keys its form/request owner by account ID; same-user transitions preserve it. AccountUpgrade confirms through /me before retrying; AccountSession owns profile/token acceptance. API tests preserve the user's ID, and replacement/disposal tests exclude stale responses and blocked new controllers | Real form typing, keyboard/focus, same-account screen cleanup and account replacement |
| 04.4.3 Result lifetime and upgrade survival | Shared GAME_OVER_LINGER_MS is 180000; durable result runtime/record expiry tests pass. The upgrade key depends only on account ID, not game/result/screen | Browser form survival through actual cleanup; exact remaining 04a dimensions |

The 04a one-command dispatcher and layout-level sheet supersede plan 04's earlier two-command chain/component placement proposal. The quick-play guest row is explicitly covered in the current matrix, while the 04a table separately names registered quick play. A historical snapshot without a usable bot difficulty offers Medium explicitly. **163 tests across seven files** and the build pass; [first-session verification](../first-session-verification.md#result-actions-and-upgrade-form-ownership) contains the exact matrix and test scope. This closes local source/rendering coverage for those rows, not manual/device acceptance or the whole 04/04a plan.

## Plan 04 phase 1.7 — empty-state and history recovery

The empty-state pass contradicted the prior personal-history implementation: it filtered a 50-game global response by username. The existing authenticated history API now supplies the player's own latest 20 games by ID, while the shared server projection keeps list identity/outcomes consistent. Real database tests cover older personal games excluded from the global window, renamed users, profile privacy and authentication. GameLogFeed tests cover early subscription, coalesced invalidations, recovery, disposal and account/view changes. Leaderboard failures are explicit errors with Retry; empty rankings, game lists and room filters have working navigation destinations in source. These changes do not add another history endpoint or game-start mechanism.

**63 tests across six files** and the production build pass. [First-session verification](../first-session-verification.md#history-recovery-and-empty-states) records limits and manual acceptance. Global chat retains its existing between-games empty copy and composer; chat delivery/recovery is a separate established owner. Physical layout, keyboard focus and hydrated empty-state buttons are not proven by the lifecycle/API tests, so plan 04/04a device acceptance remains open.

## Shared first-session request lifecycle

The follow-up audit found that withDeadline and ReadResource signalled abort without independently settling the awaited transport. Cooperative-fetch tests did not establish the promised bound when cancellation was ignored. The shared helper now owns both deadline settlement and optional owner cancellation; ReadResource, authentication form/bootstrap and account upgrade reuse it. Existing wallet/admin callers benefit without new command identities or changed server transactions. [First-session verification](../first-session-verification.md#request-deadlines-and-cancellation) defines terminal read behavior and uncertain-write limits.

The focused 70-test run covers never-settling work, late failures, retry independence, disposal, listener/timer cleanup and the affected owners. Full-suite/build evidence is recorded in the latest checkpoint. This closes the source-confirmed missing observation bound; it does not establish production network behavior, browser acceptance or the whole roadmap.
