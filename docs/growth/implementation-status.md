# Implementation status — 11 September 2026

Current priorities are consolidated in [remaining work](remaining-work.md). Historical checkpoints below preserve evidence; they are not separate queues.

## Latest checkpoint: accepted owner decisions

All six outstanding product/operations decisions are resolved in [owner decisions](owner-decisions.md). That record supersedes historical pending questions below. Expiring profile invitations, server-owned theme application and GitHub feedback links are implemented locally; the separate puzzle publisher configuration is prepared. The owner chose no open-source licence and a $10/month total hosting budget with manual monitoring. Production activation is not implied.

Final local verification: **1,108 tests / 104 files pass**, including actual socket delivery, reconnect, duplicate send, recipient acceptance and readiness before game start. Theme tests cover ownership, current projection, independent reset, stale receipts and account replacement. The production Vite build passes. A pre-existing settlement batching test now explicitly makes its stored jobs due, removing database/process clock dependence without changing settlement code. Evidence and two focused manual checks are in the decision record.

## Previous checkpoint: clean Linux verification and matchmaking deadline

A clean Node 24.21.0/Debian 12 container with isolated PostgreSQL applied all 25 migrations, provisioned three bots/fifteen shop items idempotently, and passed **all 1,095 tests across 103 files** plus the production build. An actual npm-start smoke completed guest bot play, a worker reply, reconnect, duplicate-command confirmation, resignation settlement, saved replay and native PNG generation. [Linux verification](../linux-verification.md) records source hashes, environment, artifacts and precise limits.

The initial full run exposed two failures. The matchmaking timer could wake before server wall time reached its deadline and never republish; it now rearms until due and rejects stale session/search callbacks. Seven deterministic regressions cover time and ownership boundaries. Puzzle publication tests now use the current verifier version while preserving stored generator provenance. The focused 89-test run and final full run pass.

The isolated containers/network were removed after verification. No production database, commit, deployment or outreach changed. Railway capacity/replacement, browser acceptance and the remaining roadmap are still open; local Linux success is not launch clearance.

## Previous checkpoint: strategy practice and rendered editorial checks

The computer guide now provides a concrete My games → Replay exercise. Four descriptions give clearer answers, the beginner title names opening moves, and two redundant contextual detours were removed. All twelve rendered bodies now meet their planned length ranges and contain two to four contextual destinations. The public-FAQ audit was corrected to read the actual buttons rather than an empty heading set; all 51 article questions are distinct, and no public FAQ sentence of eight or more words is reused in article paragraphs/answers.

**43 tests across three files, the production build and 367 rendered checks pass.** [Strategy verification](../strategy-verification.md#rendered-editorial-checks--11-september-2026) records source evidence, counts, logs and manual acceptance. Literal query wording, comprehensive outline/source review, device and deployed acceptance remain open. No full-suite, ranking or analytics-delivery claim is made. All owned processes completed; no production write, commit, deployment or outreach occurred.

## Previous checkpoint: puzzle editorial review

All 31 local launch explanations now have individual tactical notes, and all six shared theme paragraphs reflect the house rules without overstating forced replies or wins. Generator 1.2.3 identifies offered kings correctly and derives alternative capture/crowning/material facts from legal turns, rejecting corrupt continuations. The reviewed artifact preserves every non-commentary field and its original generation provenance.

**84 tests across six files pass**, including legal replay of all 31 solution/continuation texts and 62 alternative samples; the production build passes with the existing adapter warning. The [editorial review](../puzzle-editorial-review.md) maps every position, records evidence and explains the limits of short alternative samples. Each explanation has at least 335 words and at least 153 words in sentences not repeated in another buffer entry, excluding shared theme text. This is not an indexing or independent exhaustive-search claim.

No published row, production migration, commit, deployment or external post changed. Production publication, owner/device acceptance, runner/monitoring, feedback and the wider roadmap remain open. All owned test/build processes completed.

## Previous checkpoint: bounded request observation

The common deadline helper now settles independently of transport cooperation and accepts owner cancellation. ReadResource, authentication forms/bootstrap and account upgrade share it, replacing duplicated timeout handling. A hung request cannot prevent its read/form owner from observing timeout or disposal; old completions cannot replace newer data. Timers and abort listeners are removed on terminal paths. The helper does not decide whether server writes committed: existing receipts and account confirmation still own that question.

**All 1,052 tests across 101 files pass** (`.generated/tests-deadline-full.log`). The focused 70-test run covered never-settling work, ignored abort, late failures, pre-cancellation, synchronous exceptions, cleanup and immediate retries; the full suite additionally covers wallet/admin/friends callers and server/socket behavior. The production build passes (`.generated/build-deadline-lifecycle.log`), with the existing adapter unused-import warning. [First-session verification](../first-session-verification.md#request-deadlines-and-cancellation) records terminal behavior, evidence and manual checks. Documentation-link and whitespace verification pass (`.generated/verify-deadline-lifecycle-docs.log`, `.generated/check-deadline-lifecycle.log`). All owned processes completed.

No schema, production migration, commit, deployment or external post occurred. Browser/production acceptance and the remaining roadmap are still open.

## Previous checkpoint: personal history and empty-state recovery

GameLog no longer approximates personal history by filtering global games by name. It uses the existing authenticated history endpoint and stable participant IDs, with one shared server list projection. The new feed owner wraps ReadResource, subscribes before reading, coalesces live invalidations, refreshes on connect and disposes obsolete account/view work. Leaderboard failures show Retry instead of an empty ranking. Empty lists and filters now expose navigation through existing owners.

**63 tests across six files** pass (`.generated/tests-history-recovery.log`), including the real API case with 51 newer unrelated games and a renamed/private account. The production build passes (`.generated/build-history-recovery.log`), with the existing adapter unused-import warning. [First-session verification](../first-session-verification.md#history-recovery-and-empty-states) documents endpoint compatibility, lifecycle cases, fixed windows and concise manual checks. Documentation and whitespace checks pass (`.generated/verify-history-recovery-docs.log`, `.generated/check-history-recovery.log`). All owned test/build processes completed.

No full-suite, unlimited-history or hydrated-button claim is made. The earlier full-suite result predates these changes. The complete roadmap remains unfinished; no schema, production, migration, commit, deployment or external post occurred.

## Previous checkpoint: result matrix and upgrade form ownership

The real result sheet now has rendering checks for all six origin/account combinations, conditional search, rematch consent/departure and pending/recovery availability. Play again uses and displays the finished game's accepted difficulty instead of the mutable lobby preference. The account upgrade sheet remains layout-mounted for same-account navigation/result cleanup but is keyed by account ID, disposing obsolete requests and form fields. A captured generation also prevents obsolete success notices.

**163 tests across seven files pass** (`.generated/tests-result-matrix-integration.log`), covering actual SSR components plus account workflow/session, auth API, durable results and socket commands. The production build passes (`.generated/build-result-matrix.log`). [First-session verification](../first-session-verification.md#result-actions-and-upgrade-form-ownership) maps the complete result action matrix, superseding contract, source-confirmed fixes and remaining hydrated/device acceptance. Documentation-link and whitespace verification pass (`.generated/verify-result-matrix-docs.log`, `.generated/check-result-matrix.log`). All owned handles completed.

The earlier full 1,011-test run predates these UI changes; no new full-suite claim is made. Exact 360px result dimensions, form focus/keyboard behavior and manual command clicks still need their own evidence. No schema, production migration, commit, deployment or outreach occurred. The wider roadmap remains unfinished.

## Previous checkpoint: durable invitation entry and spectator links

Accepted code-based joins now record participant invitation evidence in the existing room membership transaction. Game reservation copies it into GameRun, and restoration supplies a participant-specific snapshot boolean. GameAnalytics uses that evidence for source invite and retains its confirmed source in the existing event journal. Hosts, directory joins, failed joins, spectators and rematches do not become new invite conversions. Older canonical room JSON remains valid; historical GameRuns default to unattributed. The `20260911140000_game_invitation_entry` migration is applied only to the isolated local test database, with production rollout still pending.

The trace also fixed an actual invitation path failure: the server spectate command now resolves a supplied code, matching navigation's Watch Game request while keeping private-room and spectator permission checks. [First-session verification](../first-session-verification.md#accepted-invitation-entry-and-game-attribution) and [durable rooms](../durable-rooms.md#accepted-invitation-entry) document ownership, compatibility and manual acceptance. GA4 delivery and the production funnel remain externally unverified; reporting must include both the public /join route and approved direct /invite QR route.

**All 1,011 tests across 98 files pass** (`.generated/tests-invitation-entry-full-final.log`), including socket reconnection, process restoration, membership/reservation transactions, rematch isolation, authentication/navigation and the event observer. The earlier focused run passed 177 tests across seven files. The production build passes (`.generated/build-invitation-entry.log`), with the existing adapter unused-import warning. Documentation-link verification and whitespace checks pass (`.generated/verify-invitation-entry-docs.log`, `.generated/check-invitation-entry.log`). All owned test/build handles completed.

The first full run exposed two stale research source hashes after the earlier puzzle-reward constant change. Both original experiment scripts were rerun. All opening results and the complete two-kings proof tree are identical to the prior artifacts; only generation timestamps and the shared constants hash changed (`.generated/compare-research-invitation.log`). The final suite independently checks the regenerated proof. No article claim changed. The initial failure log is retained as `.generated/tests-invitation-entry-full.log`.

No production migration, commit, deployment or external post occurred. The full roadmap remains unfinished.

## Previous checkpoint: analytics cohort and honest room attribution

The shared analytics boundary now derives `first_session` from GA4's session number instead of inventing another visitor/session owner. Lookup batches preserve order and captured route/parameters, bound waiting to 250ms and pending events to 64, reject obsolete callbacks, and explicitly label unavailable cohorts as unknown. New batches reread the session number. Game actions never await telemetry. The configured tag URL is the single measurement-ID source.

Room-origin starts now report `room`, correcting a misleading blanket `invite` classification. Existing journals migrate without repeating completed events. This does not complete invitation-link attribution or repair already-delivered historical reports. [First-session verification](../first-session-verification.md#ga4-first-session-cohort-and-source-correction) defines the cohort, source correction, local evidence and remaining browser/GA4/report checks.

**59 focused tests across four files**, production build, documentation-link and whitespace checks pass. Logs: `.generated/tests-analytics-cohort.log`, `.generated/build-analytics-cohort.log`, `.generated/verify-analytics-cohort-docs.log` and `.generated/check-analytics-cohort.log`. No full-suite, deployed GA4 delivery or property-configuration claim is made. The wider goal remains unfinished; no migration, commit, deployment or external post occurred.

## Previous checkpoint: shared live and replay history

Live move history still used algebraic coordinates and the public replay independently guessed capture-chain boundaries. Both now consume `shared/gameHistory.js`, which replays legal coordinates through the shared rules engine and formats turns with `shared/notation.js`. Captures, chains and crowns are derived even when older annotations are absent or wrong. Live history recalculates for corrected snapshots with an unchanged move count. This is a presentation projection; it does not write accepted session state.

Replay controls consume only the verified prefix, stop at the first invalid step and display an explicit explanation. A broken record is not silently skipped or repaired, and its incomplete board is not presented as the terminal outcome. Stored result metadata remains independently visible. Missing history is distinguished from an explicitly empty history.

**42 focused tests across five files** pass (`.generated/tests-game-history.log`), including actual replay component rendering and database/API result checks. The production build passes (`.generated/build-game-history.log`). The new legal eight-step fixture covers ordinary moves, exchanges, a two-hop capture and crowning, partial live chains, stale annotations, invalid-prefix stopping and input immutability. The full suite was not rerun.

Manual acceptance: play a game with a capture chain, confirm the live strip shows one numbered turn with every landing, then compare its saved replay. Step through the replay in both directions and confirm existing animations still work. Browser/device acceptance and the wider roadmap remain open. No migration, commit, production deployment or outreach occurred.

## Previous checkpoint: prompt lifecycle and FAQ disclosures

SharePromptView now owns promotion eligibility and one participant/game grant. It suppresses remounts, clears revoked privacy/result/account grants, updates eligible profile links and isolates optional telemetry failures. The storage cap no longer lets an old readable value override today's in-memory claim after a failed write. Pending, failed and cancelled outcomes cannot consume win promotion.

The FAQ now contains the missing guest/profile/cached-preview disclosure and updated account, coin, room and reconnection wording. The approved one-coin puzzle reward is one shared constant used by the service and FAQ. **37 focused tests across three files** and the production build pass. The actual prerendered HTML contains all eleven questions/answers matching JSON-LD, verified by an HTML parser. [Sharing notes](../share-previews.md#prompt-lifecycle-and-public-disclosures) record exact behavior, logs and remaining browser/cross-tab limits. No schema, production, commit, deployment or external post occurred.

## Previous checkpoint: preview content and crop conformance

Version-3 cards add named outcomes, move/reason details, signed ranked rating chips, invitation initials and correct wager/account copy. All essential content fits a centered square; this resolves the original plan's incompatible wide-board and square-crop coordinates. Actual-font fitting preserves minimum text sizes and uses visible ellipses for long names, while a fixed outcome label and full wager values remain visible. The shared version updates page URLs and renderer cache identity together.

**51 focused tests across five files** pass, including twelve measured/rasterized card fixtures, worker-route PNGs, actual loaders, API/SSR results and invitation text. The square gallery and full game image were visually reviewed; two issues found there were corrected and re-rendered. [Sharing and preview images](../share-previews.md#version-3-content-fitting-and-centered-crops) records the layout decision, artifacts, tests, local timings and external limits. Platform crops/caches and Railway measurements remain unverified; profile challenge behavior is still awaiting the owner's preference. No schema, production, commit, deployment or external post occurred.

## Previous checkpoint: preview admission and result projections

Eight HTTP tests now exercise both preview request budgets, all four surfaces, refill, missing/invalid identities, conditional requests and concurrent pending work. They prove over-limit requests stop before service lookup. The router accepts a test clock through the existing limiter; production defaults are unchanged. A real database/API test also verifies both rating deltas and the saved end timestamp through replay, default/public game lists and both players' profiles.

**33 focused tests across four files** pass (`.generated/tests-preview-admission-fields.log`). The [sharing requirement map](../share-previews.md#requirement-map-and-remaining-proof) now records these local evidence rows as covered. Target-host capacity/proxy behavior, challenge semantics, card layout/crop work and browser/platform acceptance remain open. The owner question distinguishes joining an open public room from adding pending challenges; no new challenge workflow or external notification was implemented while that preference is pending. No schema, production, commit, deployment or external post occurred.

The admission/projection checkpoint also passed the production build, documentation verification (29 documents, 221 local links) and whitespace checks; all test/build handles completed. Logs are recorded in the sharing document.

## Previous checkpoint: sharing contract and preview version ownership

Results now share the player's perspective, recorded move count and confirmed positive ranked rating change; own and public profiles use appropriate wording and their record. Puzzle text remains spoiler-free. Share attempts and cancellations now carry consistent item metadata, and null native failures safely reach clipboard/manual fallback. The query-bearing live-game robots rule is present.

All four preview loaders and the renderer cache now use one version constant, replacing stale page `t=1` values while the renderer used version 2. Replay metadata now includes article type and result-aware alternative text. **45 focused tests across seven files** pass, including real loader, API/render and worker PNG cases. [Sharing and preview images](../share-previews.md#share-contract-audit-11-september-2026) maps each plan-06 phase to evidence and remaining work.

The complete share loop is not yet achieved: profile Challenge still routes only to sign-in, some specified card layout/crop details are missing, rate-limit admission proof is absent, and browser/GA4/platform/Railway measurements remain open. No production, migration, commit, deployment or external post occurred.

The production build, documentation verification (29 documents and 220 local links), and whitespace check also passed. Exact logs are recorded in the linked sharing document; all owned test/build handles completed.

## Previous checkpoint: game analytics ownership

Game lifecycle reporting now consumes accepted session snapshots through one read-only observer. It waits for the server start, derives the participant's first move from accepted history, shares result classification, and scopes recent event flags by participant/game across remounts, reconnects and optional same-tab storage. Unknown restored history is baselined. Start attribution uses confirmed journeys and canonical game origin instead of global markers left by attempted commands.

**31 focused tests across two files** and the production build pass, including the real SessionClient lost-acknowledgement/recovery boundary. Documentation verification covered 29 documents and 217 local links; whitespace checks passed. [First-session verification](../first-session-verification.md) documents the source taxonomy change, storage/delivery limits, manual checks and logs. The `first_session` cohort definition, complete invite attribution, GA4 delivery, remaining result/action matrix and wider roadmap remain open. No schema, production, deployment or outreach change occurred.

## Previous checkpoint: waiting-room state and wake-lock ownership

Waiting-room readiness and room-level Start/Add Bot availability now come from shared server predicates. The UI no longer interprets ordinary waiting state as a failed start, marks offline auto-ready humans Ready, or offers bots in wager rooms. Manual ready flags and host/connection authorization remain server-owned.

ScreenWakeLock owns only the visible invitation's optional browser lock. Pending acquisitions are deduplicated and invalidated on hide, room change and disposal; late locks are released, old release events cannot clear new ownership, and optional capability failures do not block entry. [First-session verification](../first-session-verification.md) records the source contract, controlled resource tests, real database/socket evidence and phone checks still required. The full first-session matrix, result/analytics audit, actual-device acceptance and broader roadmap gates remain open. No schema, production, deployment or outreach action occurred.

Validation: **93 focused tests across three files**, the full **904 tests across 92 files**, and the production build passed. Documentation verification covered 29 documents and 215 local links; whitespace checks passed. All owned test/build handles completed. Exact logs and manual checks are in the linked verification document.

## Previous checkpoint: lobby roster and preference recovery

The bot picker now uses the existing ReadResource lifecycle and validates its public roster instead of retaining an unbounded global cache. It refreshes when reopened, cancels on disposal, exposes Retry and preserves usable difficulty fallback labels. Native radio inputs and visible focus replace simulated radios; wrapping caption text avoids the previous tiny subtitle. A denied localStorage getter no longer prevents the difficulty store from loading.

The lobby's preference owner now moves an automatically selected first-game Bot tab to Quick Play after the accepted game snapshot, while preserving explicit selections. First-time registered players receive the same Bot entry as first-time guests. Tests cover hint/session binding, Skip, saved/invalid preferences, disposal and storage denial.

**893 tests across 91 files** passed; [first-session verification](../first-session-verification.md) maps this scope to 04/04a requirements and records focused/build logs and manual checks. Actual browser radio interaction, 360px visual acceptance, the complete action matrix and other roadmap/release gates remain open. No schema change, production write, commit, deployment or outreach occurred.

## Previous checkpoint: pairing ownership and uncertain room creation

Quick-play pairing now owns exact search claims until admission or requeue. Cancel/disconnect/bot fallback invalidate them, so delayed work cannot affect a later search. An unaffected partner retains their original FIFO position, wait time and fallback deadline. Before requeueing a failed pair, the domain restores any existing durable membership.

The shared room-creation boundary confirms uncertain writes under the creation identity lock and retries projection of an accepted record. Later session commands remain ordered behind unavailable confirmation, with bounded backoff and shutdown cancellation. Changed creation intent remains an error. This serves configured, bot and quick-play rooms without adding another browser state owner or schema.

[First-session verification](../first-session-verification.md) describes behavior, tests and manual checks. The wider 04/04a action matrix, actual-device acceptance, remaining roadmap requirements and production gates stay open. No production mutation, migration, commit, deployment or outreach occurred.

Validation: **874 tests across 89 files** and the production build passed. A final server lifecycle check at installation and between nested recovery calls was then verified by **106 focused tests across four files**, including the additional committed-projection shutdown case. The linked verification document records the exact sequence and logs. Documentation checks passed for 29 documents and 211 local links; the whitespace check was clean. All owned test/build handles completed.

## Previous checkpoint: shared session ordering and guarded room admission

Human pairing now reserves both participants' session queues; socket commands and reconnect restoration use the same helper. Restoration starts after previous session work has settled. Queued gameplay captures its runtime lifetime at admission and participates in shutdown draining. Incomplete restoration cannot admit gameplay commands.

Room creation now rechecks its actor inside the database transaction, including after account reads and membership writes. Obsolete creation rolls back fully; matching committed receipts remain readable after connection replacement. [First-session verification](../first-session-verification.md) records the tests and the remaining pairing ownership/requeue work. This checkpoint does not establish recovery from a failed or uncertain pairing write, cancel/rejoin ownership, the entire 04/04a action matrix or production readiness. No schema change, production write or deployment occurred.

Validation: the full **862 tests across 88 files** and production build passed. A subsequent server-only correction keeps a replaced connection actionable when its queued pair becomes unavailable; **89 final focused tests across three files** passed, including the added replacement case. Logs and the exact verification sequence are recorded in the linked document. Documentation checks passed for 29 documents and 209 local links; the whitespace check was clean.

## Previous checkpoint: first-session authentication recovery

Started the 04/04a audit with authentication and automatic invite entry. AuthenticationFlow now owns one mounted form's requests, using existing request deadlines and AccountSession identity checks. Form changes, disposal and newer authentication attempts invalidate old results; late suggested names cannot overwrite typing. Automatic invite lookup/guest creation and saved-session restoration now receive bounded cancellation signals. Registration timeout explains sign-in recovery, and search fallback copy no longer claims zero searchers when unmatched humans exist.

Validation: **60 focused tests across four files**, the full **851 tests across 87 files**, and the production build pass. [First-session verification](../first-session-verification.md) records the scope, logs and concise manual checks. No schema change, production write or deployment occurred. The remaining first-session transition/action matrix, actual-device scan experience, analytics and other roadmap gates remain open.

## Previous checkpoint: durable puzzle rejection policy

Added the independent PuzzleRejection catalogue and migration `20260911130000_puzzle_rejections`. Conclusive evidence from an unchanged row is persisted atomically with publishing changes and survives deletion of that row. Canonical identity covers both orientations; records retain source date, reason, depth and generator/verifier versions. Retries retain stronger evidence. Generation, commit and import share the exclusion policy; a later shallow verification does not clear quarantine. Stale observations and exhausted budgets do not add rejection evidence.

Status now distinguishes complete calendar coverage from health and reports rejected dates in the inspected interval. A retained published failure continues to yield exit 5 on later status/maintenance runs, without requiring the original process report. Published puzzles remain unchanged and no automatic quarantine bypass is provided.

Validation: **838 tests across 86 files**, **42 focused tests across four files**, Prisma validation and the production build pass. Logs are in [puzzle operations](../puzzle-operations.md#local-verification). The migration was applied only to local `checkers_test`; production migration/application remains pending in [launch readiness](launch-readiness.md). No deployment, production write or outreach occurred. Editorial, actual-device, owner decisions and remaining roadmap work stay open.

## Previous checkpoint: prepared puzzle publication and midnight recovery

Removed CPU searches from the publishing transaction. Reverification and generation now prepare proposals; a shared, 30-second commit checks current records, database publication time, occupied dates and equivalent positions. It preserves public/imminently public rows and existing attempts, skips stale proposals, and rolls back a failed replacement atomically. Conclusive failures that cannot safely be removed are reported through `protectedFailures` and exit 5 even when calendar coverage is healthy. Dry-run/proposed counts are separate from actual writes.

The settled focused run passed **39 tests across four files** (`.generated/tests-puzzle-publishing-final.log`), including actual PostgreSQL mutation/rollback cases and pure preparation tests for budget exhaustion and the depth cap. The production build passed (`.generated/build-puzzle-publishing-release.log`). The final full-suite result is recorded in [puzzle operations](../puzzle-operations.md#local-verification). The first broad run overlapped a test edit and loaded an older service version; its failed fixture was removed by exact ID/hash from local `checkers_test`, and fixture cleanup now also tracks replacement hashes. No production mutation occurred.

Durable exclusion of failed positions across later independent runs remains a release requirement; same-run exclusion is implemented. Editorial/device/owner decisions, production activation and the broader roadmap remain open.

## Previous checkpoint: puzzle search comparison and import boundary

Import now verifies board and metadata structure, exact legal turn annotations, generator-derived difficulty/themes, adequate verification depth and UTC publication dates before accepting reviewed content. It preserves the existing publishing transaction and conflict policy. Invalid capture/promotion annotations and discontinuous chains can no longer accompany an otherwise recognized solution.

**61 focused tests across six files passed**, including an independent exhaustive depth-three comparison for all 31 launch positions, a stronger non-first capture branch, competing winning choices and inconclusive budget exhaustion. The actual import CLI reverified all 31 launch puzzles at their recorded depths and exited without writing rows. Exact scope, logs and remaining plan-03 gates are in [puzzles.md](../puzzles.md#search-and-import-verification--11-september-2026). This pass changed scripts/tests/docs; it did not deploy or modify production data. Broader roadmap work remains active.

## Previous checkpoint: puzzle sources and factual position commentary

Generator 1.2.2 now shares public-game eligibility and pagination with discovery. Generator and API share complete replay validation; attribution requires a matching board and side to move in an eligible replay. The page links the verified source using public participant names. Source-backed responses revalidate on every request through `no-store`, including when attribution is currently unavailable.

Corrected position descriptions that treated weighted evaluations as actual pieces, and removed tactical claims inferred only from advanced-piece counts. All 31 local launch-buffer position paragraphs were corrected while preserving positions, solutions, dates and original generator provenance. Existing database content was not overwritten. **37 focused tests across six files** and the production build passed; details are in [puzzles.md](../puzzles.md#generating-and-publishing). No production write, deployment, commit or outreach occurred. Remaining generator criteria, operations and roadmap requirements stay open.

## Previous checkpoint: puzzle metadata, breadcrumbs and guide discovery

Closed the page requirements identified by the preceding audit. Dated puzzles now have date/side titles and difficulty/theme/date/side descriptions, WebPage publication/reveal dates, full visible/schema breadcrumbs and published head prev/next links. PublicIndex uses one parent list for both breadcrumb forms; other public indexes retain their two-item hierarchy. A shared theme presentation mapping supplies existing guide links on the board and below it, and consistent archive labels. The evergreen hub copy now explains promotion ending a capture turn. The old standalone archived Article block was replaced by the required WebPage schema.

Validation: **22 focused tests across four files**, a fresh production build, and **106 parsed checks across 16 local built-app HTTP responses** pass. Current/archive/empty/future pages, spoiler boundaries, real generated PNGs, sitemap dates and discovery links were checked. The current card was visually inspected. The fixture runner used only local `checkers_test`, removed its three created rows by ID and closed its HTTP server, preview worker and database connection. All test/build handles finished. Logs and scope are in [puzzles.md](../puzzles.md#page-metadata-and-discovery--11-september-2026).

This is local evidence, not deployment, indexing or actual-device acceptance. Generator/commentary requirements, real-game attribution, production buffer/runner, owner feedback choice and remaining roadmap audits are still open. No production write, commit, deployment or outreach occurred.

## Previous checkpoint: puzzle confirmation and date recovery

Plan 03 revalidation found unbounded progress requests, weak acknowledgement/history validation, local solved flags surviving authoritative reveal rejection, and midnight reload failures being acknowledged as successful. These paths now use the existing deadline/profile ownership, validated server progress, explicit Retry/storage feedback and successful-reload-only date advancement. A pending newer attempt remains pending when an older acknowledgement arrives; account/authentication changes and disposal reject obsolete work. Server reveal history also corrects the controller's local solved feedback.

Puzzle reward writes now take the shared account lock and recheck retirement before the existing identity lock. The single transaction still covers attempt, wallet, ledger and reward marker. Added tests exercise a real retirement race and ledger-failure rollback. No reward amount, puzzle content, migration or schema change was made.

Validation: the full suite passed **793 tests across 81 files**; the final focused run passed **32 tests across four files**, including actual PuzzleController playthroughs of all 31 launch puzzles and server validation of their submitted turns. The final production build passed. Logs and manual checks are in [puzzles.md](../puzzles.md#recovery-verification--11-september-2026). All test/build handles finished. No production writes, commit, deployment or outreach occurred.

The expanded [requirement audit](requirement-audit.md) maps this evidence to plan 03 while retaining unresolved page metadata/schema/breadcrumbs, theme links, generator/commentary conformance, manual acceptance and production buffer/runner gates. The broader goal remains active.

## Previous checkpoint: shared strategy catalogue and intro placement

Closed the pipeline gaps identified by the previous pass. `strategyCatalog.js` is now the common metadata/index contract for page builds and the sitemap manifest. Invalid dates, unknown/duplicate related references, malformed image URLs and unsupported translation metadata fail consistently before publication. New contract tests compile all twelve real article introductions as well as invalid fixtures and navigation boundaries.

The strategy mdsvex plugin now inserts a shared Play link after the introduction and before the first H2, replacing the header link. Authors do not embed tracking logic. Both article CTAs use the existing analytics helper; no browser DOM manipulation was added.

Validation: 49 focused tests across four files and the production build pass; 367 checks parse actual rendered article/hub HTML and the catalogue/manifest, including every prev/next boundary and CTA placement. Five read-only local HTTP responses confirm hub/article/sitemap success, unknown article 404 and blog 301. Logs and exact scope are in [strategy-verification.md](../strategy-verification.md#catalogue-and-intro-placement--11-september-2026). The temporary server and all test/build handles finished. No production write, deployment, commit or outreach occurred. Remaining editorial, Spanish, device and release requirements stay explicit in [requirement-audit.md](requirement-audit.md); the full goal remains active.

## Previous checkpoint: plan 02 pipeline and rendered article audit

Extended [requirement-audit.md](requirement-audit.md) with all ten pipeline items, the twelve-guide evidence matrix and explicit remaining editorial, translation, browser and release requirements. This is a scoped verification pass, not full roadmap completion.

- The fresh production build passed. All twelve guide sources provide 29 legal diagrams; 22 focused tests across strategy diagrams, keyboard navigation and SEO head behavior passed. Added rejection coverage for an immobile starting side.
- Parsed the actual twelve prerendered article pages: 316 checks passed for metadata/schema, headings, FAQ parity, navigation links and SSR diagrams/screenshots. Artifacts: `.generated/strategy-rendered-audit.json`, `.generated/audit-strategy-rendered-final.log`, `.generated/audit-strategy-source-final.log`, `.generated/tests-strategy-audit.log`, `.generated/build-strategy-audit-final.log`.
- Added missing contextual FAQ links in eight guides and setup links in two rules guides, updating their edit dates. Removed stale hub publication copy and routed its CTA through the shared analytics helper.
- Remaining conformance includes leading CTA placement, direct index/frontmatter contract tests, precise editorial requirements and the future Spanish architecture. Browser/device acceptance, GA4, deployed URLs and Search Console remain unverified by this pass. No deployment, production write, commit or outreach occurred; the broader goal remains active.

## Previous checkpoint: plan 01 requirement audit

Created [requirement-audit.md](requirement-audit.md), mapping every numbered implementation item in plan 01 to current evidence and remaining proof. This is the first part of the full completion audit; plans 02–06, the 04a ruling and the broader reliability matrix still require equivalent review. No full-goal completion is claimed.

- Inspected current routes/loaders/layouts, shared discovery policy, metadata components, sitemap implementation and relevant test assertions. Confirmed the git index has no tracked build/site files, preserving prior staged removals. Visually inspected the actual share image and inspected its Poppins generator.
- **112 checks passed over 17 local HTTP responses** from the real Express app and built Svelte handler, using only read access to the isolated test database. Parsed actual HTML/JSON-LD/XML and PNG headers to verify status/redirects, headings, metadata multiplicity, languages/alternates, ten FAQ answers/questions, discovery navigation, asset size/dimensions, manifest icons and sitemap content/cache. Artifacts are `.generated/seo-http-audit/audit.json`, `responses.json` in that directory and `.generated/audit-seo-html-final.log`.
- **31 focused tests across four files passed** (`.generated/tests-seo-audit.log`) for SEO head escaping/robots rules, sitemap behavior, real-DB discovery/privacy policy and server-rendered profile/replay results. The current production build passed (`.generated/build-seo-audit-final.log`) with the existing adapter unused-import warning. The prior full 756-test run remains the earlier privacy checkpoint; it was not unnecessarily repeated for these small title/link edits and documentation work.
- Closed three literal plan omissions: app layout tab title, leaderboard links in both error sources, and the explicit ELO leaderboard title. The latter two are checked in actual HTTP HTML. The app intentionally disables SSR; its hydrated title and closed mobile menu are source/build evidence with browser acceptance still pending.
- Initial audit failures were retained. The local fixture needed an explicit HTTP origin because adapter-node defaults to HTTPS; the audit also incorrectly expected a closed mobile menu in initial HTML. Those fixture/expectation issues were corrected. The temporary server, connections and all test/build handles finished; no migrations, seed, fixtures, gameplay jobs, production requests, commits, deployments or outreach ran in the HTTP pass.
- The audit preserves every named external verification gate: deployed URL/crawler checks, Search Console, structured-data validators, real social previews and mobile Lighthouse. It identifies obsolete stub-only expectations instead of restoring noindex to a populated strategy hub. Documentation/changelog and current overview/launch links are updated; remaining plans and owner/external gates are explicitly queued.

## Previous checkpoint: revision-conditional profile privacy

Revalidation of plan 06 Phase 3 item 2 found a remaining unconditional privacy write and an unbounded component confirmation path. This pass closes that specific source-confirmed gap. It is not a completed requirement-by-requirement audit of the full roadmap.

- `server/services/profileSettings.js` now owns the setting transition. The authenticated route requires the browser's accepted `expectedProfileVersion`, locks the account with the shared account-lock helper, rechecks retirement and rejects stale revisions with 409. The existing transactional User trigger advances the revision even for a same-value explicit choice, preventing an older pending write from overriding it. Unrelated User writes conservatively cause a conflict rather than being lost.
- `ProfileSettingsClient` keeps the checkbox bound to the account owner, blocks duplicate input through write/read confirmation, uses the shared 15-second deadline and refreshes current state after success, rejection or uncertainty. It never installs historical write responses or automatically retries a privacy choice. A failed profile read distinguishes confirmed from uncertain writes, blocks further edits and exposes Refresh account. Account generation and disposal suppress old feedback/follow-up reads and prevent an old completion from clearing a new action's busy state.
- Absolute privacy settings use the existing profile revision instead of another command receipt table, queue or privacy counter. The shared deadline helper accepts context-appropriate timeout copy while retaining existing defaults for other workflows. No migration or dependency was added; public profile/preview enforcement and the disclosed leaderboard/shared-game-name visibility policy remain unchanged.
- **756 tests across 79 files passed** (`.generated/tests-privacy-full.log`), including **35 focused checks across five files** (`.generated/tests-privacy-focused.log`). New service tests cover delayed/same-value/concurrent decisions, unrelated profile changes, rollback, retirement and strict API revisions. Client tests cover uncertain/malformed responses, deadline expiry, read recovery, duplicate input, account replacement and disposal. Existing leaderboard/preview privacy integration checks were updated to supply the accepted revision. The production build passed (`.generated/build-privacy.log`) with the existing adapter unused-import warning.
- [profile-state.md](../profile-state.md#privacy-writes) records the contract and concise manual checks. Deploy server/client together and reload old tabs: old unconditional privacy requests are rejected. Existing profile-version migration requirements remain. Changelog, deployment, audit and plan 06 status notes were updated; documentation links and whitespace checks passed. No production writes, commit, deployment or outreach occurred.
- Remaining: the full roadmap completion audit; pending theme policy and unified application; broader browser/device acceptance; target capacity/Linux/Railway validation; production puzzle publication/runner/monitoring; owner decisions, release and external launch gates. The earlier skin/Friends/profile checkpoints retain their verified local scope.

## Previous checkpoint: equipped piece appearance

Equipped skins now reach live player, spectator and app replay boards through one account-scoped appearance read. Standard pieces can be selected again through the existing equipment command. This closes the source-confirmed skin rendering gap; theme policy/application and the wider roadmap remain incomplete.

- `server/services/appearance.js` reads only the authenticated account's equipped SKIN and palette in a Repeatable Read transaction. It returns an explicit standard-piece selection, rejects ambiguous legacy equipment and rejects malformed palette metadata. Reads never repair inventory or disclose other users' selections. No-store responses use the existing Prisma pool.
- `shared/pieceSkins.js` normalizes the seed palette format and supplies both the canvas and replay CSS renderers. `canvasPiece.js` now draws every stationary, moving, captured, dragged and crowned piece with the selected palette, retaining geometry and animation inputs. Palette changes trigger redraw without modifying game snapshots or restarting transitions. Labeled red/black swatches explain skins whose visible colors differ from their canonical side names.
- The app owns one appearance lifecycle using `ReadResource`, with account-generation reset, ordering, deadlines, disposal, focus/session recovery and explicit Retry. Failed reads leave gameplay usable with standard or previously accepted pieces. Equipment confirmation reads current appearance rather than applying a historical receipt. Reusable replay rendering takes an explicit palette prop; public replay rendering does not import account services. Marketing/puzzle/article/share-image boards remain standard presentation.
- Standard-piece selection is `{ itemId: null, itemType: 'SKIN', requestId }` on the existing equip route/kind. It clears only skins under the existing account lock and persists a matching reset receipt atomically. It cannot change themes, debit coins or erase a newer choice on replay. The existing wallet journal handles uncertain reset/reload confirmation; no new mutation queue or receipt table was added.
- **740 tests across 77 files passed** (`.generated/tests-skins-full-final.log`). Coverage includes private entitlement reads, actual equipment retries, ambiguous/malformed selections, reset rollback, matching journal receipts, stale/account/disposal reads, lifecycle cleanup, canvas palettes/effects and server-rendered replay palettes/results. The first full run exposed the reusable replay's accidental browser-service dependency; that boundary was corrected and the full suite rerun successfully. The final production build passed (`.generated/build-skins-release.log`) with the existing adapter unused-import warning.
- [piece-appearance.md](../piece-appearance.md) records the rendering/read/reset contract and concise manual checks. Server/client must deploy together and old tabs reload for standard-piece intents/receipts. No migration, dependency, price/ownership, theme, refund, reward or production change was made. No commit, deployment or outreach was performed. Changelog, shop/wallet/deployment/audit and launch-readiness documentation were updated.
- Remaining: the pending owner free/paid theme choice and unified theme application; actual-browser skin contrast/layout and broader device acceptance; capacity/Linux/Railway validation; production puzzle publication/runner/monitoring; release and owner/external launch gates. Resource reads recover on defined lifecycle events, not a continuous cross-tab equipment stream. This checkpoint does not complete the roadmap.

## Previous checkpoint: friendship transitions and recovery

Friends now shares the established read/action lifecycle and uses durable relationship receipts. The prior stale socket-field presence check, independent pending-list read and unsafe relationship retries are replaced. The full roadmap remains incomplete.

- `server/services/friendships.js` owns one private accepted/incoming/outgoing snapshot and request/accept/remove transitions. Both participants are locked using the existing economy account order, then mutable state is rechecked. Reciprocal requests remain pending rather than implying acceptance. Bots, retired players, invalid actors/targets and changes to blocked relationships are rejected.
- `20260911120000_friendship_operations` removes legacy self-pairs and opposite-direction duplicates conservatively (BLOCKED, then ACCEPTED, then PENDING, oldest row within status), adds unordered uniqueness/self-pair constraints and creates the receipt table. Exact retries return historical confirmation without recreating removed relationships or deleting newer ones. The effect and receipt commit or roll back together. This migration has only been applied to the isolated test database; production cleanup review/application remains pending.
- Friends uses `ReadResource` for deadlines, cancellation, failed/malformed reads, account generation and disposal. `FriendshipJournal` specializes `ActionJournal` for persisted uncertain intents and matching receipt confirmation; tips retain the wallet journal. Success reads current relationships instead of applying historical receipts. Incoming requests can be declined; outgoing requests can be cancelled. Presence projects the current session connection and phase. Existing presence heartbeats refresh visible Friends views without a new timer, queue or presence store.
- **725 tests across 74 files passed** (`.generated/tests-friends-full.log`), including **75 focused checks across six files** (`.generated/tests-friends-focused-final.log`). New checks cover reciprocal contention, old/new relationship replay, permissions, rollback, private snapshots, presence, strict API targets, migration cleanup/constraints, storage/reload, malformed receipts and browser lifecycle boundaries. The first focused run caught an incomplete test game context; adding the required game color exercised a valid session transition. The final production build passed (`.generated/build-friends-final.log`) with the existing adapter unused-import warning.
- [friends-state.md](../friends-state.md) records the command/read contract, migration policy and concise manual checks. Deploy server/client together and reload old tabs: `/api/friends/pending` is removed, mutations require UUIDs and DELETE now returns JSON. No dependency, price/ownership/reward change, production write, commit, deployment or outreach was performed. Existing work is preserved.
- Remaining: equipped skin/theme application and the pending owner theme policy; broader browser/device acceptance; capacity/Linux/Railway validation; production puzzle publishing/runner/monitoring; release and owner/external launch gates. Passing this suite does not complete the roadmap.

## Previous checkpoint: emote entitlement and transient delivery

The previous shop pass identified that player emotes trusted arbitrary client content and had no inventory-read recovery. This pass replaces that path with canonical catalogue content, shared free/owned eligibility and acknowledged transient sends. Cosmetic rendering, Friends recovery and the broader roadmap remain open.

- `server/services/emotes.js` supplies one free-or-owned query for the authenticated list and each send. Invalid/non-emote catalogue data and unowned paid reactions are rejected. The browser sends an item ID, and the server publishes the stored emoji/label. Human and bot broadcasts now carry game context.
- The domain checks active player membership, connection, started/terminal/recovery state and captured gameplay lifetime before and after lookup. Runtime replacement/departure suppresses late sends. One pending lookup per player and a two-second attempt interval cover unsuccessful lookups too. Async transport errors receive a recoverable acknowledgement instead of becoming unhandled rejections.
- GameScreen uses an emote controller and the shared read lifecycle, with account/game/connection guards, deadlines, reconnect/focus reads and explicit errors. An uncertain acknowledgement never queues automatic retransmission. Old-game broadcasts are ignored and terminal/disconnected views clear bubbles. Removed the unreachable duplicate toolbar; active controls wrap with 44-pixel targets and named buttons, retaining reduced-motion behavior.
- **696 tests across 71 files passed** (`.generated/tests-emotes-full.log`), including **89 focused checks across four files** (`.generated/tests-emotes-focused-final.log`). The first focused run exposed a fixture assumption about concurrent insert order; the corrected test asserts actual ID order. Real Socket.IO coverage verifies forged/unowned rejection, canonical peer broadcasts, cooldown and async-failure recovery. The final build passed (`.generated/build-emotes-final.log`) with the existing adapter unused-import warning. Browser/device acceptance remains manual.
- [emotes.md](../emotes.md) documents semantics and concise acceptance checks. Server/client must deploy together and old tabs reload for the changed send/ack/broadcast contract. No migration, dependency, pricing/ownership change, production write, commit, deployment or outreach was performed. Documentation link and whitespace checks passed.
- Remaining: equipped skin/theme application and the pending free/paid theme decision; Friends lifecycle and presence; wider manual acceptance; target capacity/Linux/Railway validation; production puzzle publishing/monitoring; owner/external launch gates. Ephemeral emote acceptance is not proof of the whole roadmap or of delivery to every disconnected recipient.

## Previous checkpoint: shop reads and durable equipment recovery

Shop now uses a coherent catalogue/ownership read and the shared browser read lifecycle. Equipment selection uses the existing wallet action journal/receipt infrastructure, closing a delayed-retry path that could undo a newer selection. Cosmetic rendering and the full roadmap remain incomplete.

- Authenticated `/api/shop` reads catalogue and inventory from one Repeatable Read transaction. The view uses the same read lifecycle as Treasury for deadlines, cancellation, latest-request ordering, account generation and disposal. Initial failure and failed refresh are explicit, malformed responses are rejected, and a confirmed action with a failed inventory read has distinct feedback.
- `equipItem` now requires a UUID and commits its immutable item receipt with the equipment writes under the account lock. Duplicate receipts cannot change newer equipment, conflicting reuse is rejected and rollback includes the receipt. Equipment does not debit coins or record a wallet transfer; emotes are not equippable.
- Equipment uses the existing wallet namespace and recovery panel, with Confirm selection wording. The journal blocks a second selection/purchase/tip while an action is uncertain; current inventory comes from a fresh read. Wallet/admin journals now observe authentication generation even for the same account ID. Recovery callbacks ignore disposed components and replaced accounts.
- **675 tests across 68 files passed** (`.generated/tests-shop-recovery-full.log`), including **58 focused checks across six files** (`.generated/tests-shop-recovery-final-focused.log`). The production build passed (`.generated/build-shop-recovery.log`) with the existing adapter unused-import warning. Coverage includes concurrent catalogue/ownership changes, account isolation, old/new equipment retries, rollback, malformed receipts, reload and view/account lifecycle boundaries. Browser acceptance remains manual.
- [shop-state.md](../shop-state.md) records rollout and concise manual checks. Server/client must ship together and old tabs must reload; equipment receipts reuse the existing table, so no migration/dependency was added. No commit, deployment, production write, pricing/ownership change or outreach was performed.
- Newly confirmed open implementation work: fixed canvas colors ignore equipped skins; paid shop themes overlap the free Profile palettes and are not applied; GameScreen's emote read lacks the new lifecycle and emote sends trust client-supplied content without ownership validation. The owner theme-policy question is pending. Friends still needs lifecycle/presence review. These remain implementation gaps, alongside broader manual, capacity/Linux/Railway, puzzle publishing and owner/external launch gates.

## Previous checkpoint: coherent treasury reads and claim recovery

Revalidation found that the browser disabled funded claims using vault funds net of every pending reward, while the actual claim service debits the vault balance. Independent aggregate queries also allowed inconsistent totals, deferred payouts were absent from paid-reward totals, and active wager reservations were missing from accounted coins. These gaps are now addressed; the broader roadmap remains active.

- `server/services/treasury.js` returns public totals and authenticated personal rewards from one Repeatable Read snapshot. It includes deferred payouts once paid, both stakes from OPEN GameRuns and deterministic bounded activity logs. Reads no longer create/write the shared vault. Account-specific responses are non-cacheable. The UI labels reservations, reward counts and player accounts accurately, and no longer calls an aggregate “total coins ever created.”
- Each pending reward carries server-defined availability. The existing transactional conditional debit and durable payout receipt remain authoritative under contention and retries; no new reward, mint, reservation or command identity was added. The component no longer calculates eligibility from `vault.available`.
- Treasury has a dedicated read/claim controller using reusable cancellation, latest-read and account-generation guards. Failed initial reads leave loading and offer Refresh; failed subsequent reads retain the last view. Confirmed claims trigger separate treasury/profile reads, with explicit feedback if either read fails. Duplicate clicks, malformed/lost responses, account replacement and disposal have controller coverage. Mutations are not automatically retried and receipt amounts are not applied to browser balances.
- **659 tests across 67 files passed** (`.generated/tests-treasury-full-final.log`). Subsequent wager-accounting additions passed **44 focused tests across five files**, including reservation, refund and settlement (`.generated/tests-treasury-final.log`). The final production build passed (`.generated/build-treasury-final.log`), retaining the existing adapter unused-import warning. Browser checks and production capacity remain pending; [treasury-state.md](../treasury-state.md) records the full contract and concise acceptance scenarios.
- Server/client rollout must be coordinated: authenticated `/api/treasury` replaces the browser's separate pending-list request; `/api/treasury/my-pending` and unused list/log accessors were removed. No new migration/dependency, production write, commit, deployment or outreach was performed. Existing work is preserved.
- Documentation verification passed for 24 documents and 145 local links (`.generated/verify-treasury-docs.log`); whitespace checks passed after removing two trailing blank lines.
- Next concrete resource gaps remain in Friends and Shop: their view reads/actions still need lifecycle ordering/recovery review. Actual-browser acceptance, target capacity/Linux/Railway verification, production puzzle publishing/monitoring and owner/external launch gates also remain open.

## Previous checkpoint: live account revision reconciliation

Connected browsers now discover account changes made elsewhere through repeated database revision hints. This extends the existing profile owner; it does not add payment/reward hooks or another source of account state. The full roadmap remains active.

- The server checks connected account revisions every five seconds using the existing Prisma pool, in non-overlapping sequential batches of 500. It reads only IDs/revisions, skips empty audiences, ignores replaced/disconnected sockets, retries failed scans and drains on shutdown. Repeated hints cover missed messages and failed browser reads without a notification ledger.
- The browser fetches only when behind, coalesces background requests, follows newer revisions arriving during a read and retries failures on later hints. The existing account owner guards publication; generation changes and listener disposal stop follow-up work. Explicit confirmations still start fresh reads after mutations. Normal latency is about five seconds plus request time, not instantaneous delivery.
- **640 tests across 64 files passed** (`.generated/tests-profile-reconciliation-full.log`), including 20 focused checks across three files. Coverage includes committed versus rolled-back revisions, repeated hints, batching, disconnected/replaced sockets, overlap, database failure recovery, shutdown, read coalescing and account lifecycle boundaries. Browser and production capacity acceptance remain pending.
- The final production build passed (`.generated/build-profile-reconciliation-final.log`); documentation link verification and whitespace checks passed. The build retains the adapter's unused internal import warning.
- No new dependency, database connection, migration, production write, commit, deployment or outreach was needed. [profile-state.md](../profile-state.md) and deployment documentation describe query/event costs and a concise two-account manual check. Existing profile-version rollout requirements still apply.
- Remaining: other resource-list ordering and manual acceptance; target capacity/Linux/Railway verification; production puzzle publishing and monitoring; owner/external launch gates. Passing this suite does not close those requirements.

## Previous checkpoint: authoritative account profile ordering

The previous pass verified clean setup. Revalidation found independent profile writers in Friends and Treasury: an older read could overwrite a newer profile, and a repeated claim receipt could inflate the displayed balance. This pass replaces those writes with one version-aware account owner. The full roadmap remains active.

- Every User update advances a transactional database revision, including bulk and raw wallet SQL. Domain services retain business policy; the trigger supplies ordering metadata. `/auth/me` is non-cacheable and returns User fields only, with inventory remaining on the shop endpoint.
- Read-only browser stores project one account owner. Old revisions, wrong-account payloads and responses from a previous authentication generation cannot overwrite current state. Equal revisions preserve frozen objects. Renewal and in-place guest upgrades preserve socket identity; late renewal tokens cannot replace a newer one. Invitation bootstrap also respects a newer authentication attempt.
- Friends, privacy and treasury use the shared refresh path. Treasury claims no longer add a saved receipt amount to local balance; failed confirmation reads are distinguished from claim failure. Friends/Treasury clear account-specific lists on identity replacement. Socket recovery/channel transitions, settlement and focus refresh through the same owner.
- **629 tests across 63 files passed** (`.generated/tests-profile-full.log`). Final focused verification passed **56 tests across five files**, including the added bootstrap race, and the final production build passed (`tests-profile-final-focused.log`, `build-profile-final.log`). Browser acceptance remains manual; [profile-state.md](../profile-state.md) lists three concise checks and explicit limitations.
- Migration `20260911100000_profile_versions` was applied only to the isolated test database; Prisma Client was regenerated. Updated rollout, audit and changelog documentation. No production migration, commit, deployment or outreach was performed.
- Remaining: immediate cross-browser account invalidation and other resource-list ordering, manual app acceptance, target capacity/Linux/Railway verification, production puzzle publishing and monitoring, and owner/external launch gates. This does not reclassify those requirements as complete because the profile tests pass.

## Previous checkpoint: clean setup and portable startup

The previous pass completed puzzle operations commands. This pass verified the documented source setup in a separate copy with fresh dependencies and empty disposable databases, exposing failures hidden by the established workspace. The full roadmap and production launch gates remain active.

- Fresh installation now explicitly generates Prisma Client. A missing generated client had caused seed and backend-test failures. The install step was also checked without `.env` or `DATABASE_URL`. Migrations and seeding ran successfully after the correction, and repeat seeding retained three bots and fifteen shop items.
- Production startup sets mode through a JavaScript entry point instead of Unix-only shell syntax. The example environment leaves `NODE_ENV` unset and uses local URLs. Development proxies follow the configured backend port, Vite requires its configured client port and the backend watcher excludes generated frontend files. Local production and test-database prerequisites are now explicit in README/deployment documentation.
- A scoped override updates Prisma's config merge dependency to the patched 8.0.0 release. npm audit reports zero known vulnerabilities; direct ordinary/cyclic merge checks and Prisma generation/migration/build/startup passed. This does not claim comprehensive security coverage.
- **618 tests across 61 files passed in the clean copy**, followed by a successful production build and `npm start` on Windows. Development and production HTTP checks covered home, health, bots and the friends guide; each mode accepted a new guest WebSocket and delivered an authoritative initial snapshot. [setup-verification.md](../setup-verification.md) records versions, source evidence, initial failures and exact limitations. The Vite configuration's subsequent difference was formatting only.
- The existing local PostgreSQL container was reused with two new databases, both now dropped. Temporary servers are stopped. The separate source/dependency copy remains under `.generated/setup-20260911` because automatic approval review rejected its recursive removal as blocked by policy; no further reason was supplied. No user data, production database, deployment or external channel was changed.
- Remaining: select/review the release, verify Linux/Railway installation and host capacity/replacement, complete manual administration/gameplay and wider profile-publication checks, activate puzzle publishing/monitoring after approval, and resolve owner/external launch gates. Local clean setup does not establish deployed or actual-device acceptance.

## Previous checkpoint: puzzle operations and calendar coverage

The previous pass completed administrative transaction checks. This pass adds the missing operational entry points for puzzle publishing; production import, runner selection/activation and monitoring ownership remain open.

- `puzzles:status` reads dated coverage without loading solutions or writing rows. `puzzles:maintain` reuses the existing generator and publishing lock, defaults to seven-day reverification and a thirty-day future horizon, then checks actual coverage after commit with a fresh UTC date. Both commands close their database connection and exit with documented status codes.
- Generator and status share one calendar policy. A later row cannot hide an earlier hole. The default requires today plus thirty future dates. Dry-run reports distinguish proposed candidates from committed rows; lock contention, incomplete coverage and execution failure remain distinct outcomes.
- Import retries now compare solution/content as well as position, rejecting conflicts atomically. A previously deepened verification level is preserved. Corrected the stale generator comment that described shifting other dates after deletion; actual behavior retains URLs and fills holes.
- Focused verification passed **29 tests across three files** before the final dry-run report wording refinement. Actual CLI runs against the isolated test database emitted shortage reports and exited with code 3 with generation/reverification disabled. These checks do not claim a healthy production buffer or a successful scheduled job.
- Final verification: **618 tests across 61 files passed**, and the production build passed (`.generated/tests-puzzle-operations-full.log`, `.generated/build-puzzle-operations.log`). Documentation verification checked 21 documents and 127 local links; whitespace checks passed. CLI help exited 0 and invalid status options exited 2. The build retains the adapter's unused internal import warning.
- Added [puzzle-operations.md](../puzzle-operations.md), deployment and changelog updates, and proposed separate Railway service settings checked against current official documentation. No service, schedule, production import, migration, commit, deployment or outreach was performed. The optional runner-choice question remains pending; prepared settings do not imply approval or activation.
- Remaining: clean setup, capacity criteria and real host/replacement acceptance; admin/manual and wider profile-publication checks; production puzzle import and scheduled execution/monitoring; owner decisions and external launch gates. The full goal remains active.

## Previous checkpoint: administrative transactions and recoverable commands

Administrative coin, rating, statistics and permission changes now have one service boundary and durable actor-scoped receipts. The approved daily puzzle reward remains one coin for a registered player's first eligible solve of today's UTC puzzle; archive solves receive none. That existing reward mechanism was not duplicated.

- Administrator and target accounts use the same ordered locks as wallet/game admission. Authorization is checked inside the transaction. Coin changes use the ledger, with balance, audit and receipt committing together. Concurrent duplicates and lost responses confirm the original action; conflicting reuse is rejected. Exact self-revocation retries remain confirmable without allowing new privileged actions.
- Shared input/profile policy rejects coercive values, preserves reward watermarks and blocks rating/stat resets while room/result membership or an unsettled game exists. HTTP handlers delegate these rules to the service.
- Administration and payments now specialize one browser action journal with separate namespaces and preserved wallet storage keys. The admin panel retains uncertain commands across same-tab reload, shows the saved target and supports explicit confirmation. Account-generation guards prevent old responses updating the next account. Browser acceptance remains manual; no browser check was performed for this pass.
- The first full run passed 603 checks and failed two existing result-test cleanup hooks because pending payouts prevented deleting fixture users. Cleanup now removes those fixture payouts before their users; final verification is recorded below.
- Final verification: **605 tests across 60 files passed**, and the production build passed (`.generated/tests-admin-full-final.log`, `.generated/build-admin-final.log`). Documentation link checks and whitespace checks passed. The build retains the adapter's unused internal import warning. These results do not substitute for the manual admin and Railway checks.
- Added [admin-actions.md](../admin-actions.md), rollout notes and changelog entries. Migration `20260911093000_admin_operations` was applied only through isolated test setup. No production migration, commit, deployment or outreach was performed.
- Remaining: admin manual acceptance and wider profile-publication ordering, clean setup and capacity criteria, puzzle publishing/runner, and owner/production launch gates. Audit receipts have no history UI and do not backfill legacy changes. This checkpoint does not close the full roadmap.

## Previous checkpoint: friends walkthrough and browser-discovered recovery fixes

All twelve planned English strategy guides now exist. The friends guide supplies the required three actual annotated screenshots, and README has a current local gameplay capture. The full roadmap remains active: content completion does not close administration, publishing operations or production/owner launch gates.

- The walkthrough covers the real Rooms → Create Room path, both-player readiness, invitation links/QR, Nearby's automatic readiness, phone controls and the distinction between free and unrated games. It passed independent review at **94/100 with zero P0**, with a 1,019-word body and three small WebP assets. Phone readers can open images at full size.
- Actual two-guest testing exposed a standard-room Creating freeze: ChatClient repeatedly published read state when the UI reported visibility. Visibility/read updates are now idempotent, with a reentrant-subscriber regression. The shared dialog explicitly centers despite the global margin reset, and the production handler import uses a file URL on Windows.
- **584 tests across 58 files passed** after those fixes. A later shared-link fallback change passed **28 focused tests across four files**, and the final production build passed. Failed native sharing/clipboard now exposes the real attributed target in a selectable input, replacing the incorrect browser-address advice. A browser denial test verified the selected invitation and its HTTP 200 response.
- Two isolated guest browsers created/joined/readied a private room and played four moves; the opponent's first move arrived without refresh. The game ended by resignation and result dismissal. The built article fits desktop/phone viewports and its three assets/full-size links work. [strategy-verification.md](../strategy-verification.md) records exact scope and logs. These are local checks, not comprehensive actual-device or Railway acceptance.
- Updated content/launch documentation, chat/sharing/player-audit contracts, README and changelog. No production migration, commit, deployment or external outreach was performed. All temporary servers, previews, tabs and emulation were cleaned up.
- Remaining: inspect the broader administration requirements against the roadmap; finish clean-setup/launch artifacts and capacity criteria; close puzzle publishing/runner work and obtain the pending owner/production evidence. Do not count those gates as complete because the guides or existing tests pass.

## Previous checkpoint: traps guide and executable defensive branches

Eleven of twelve planned English strategy guides now exist. The traps guide adds five interactive examples and meets the planned body-length range. It distinguishes forced gains from threats, conditional capture lines, paid promotion and immobilization; the original working title's blanket piece-winning claim was corrected. The full roadmap remains active.

- Four additional regression cases verify all breeches replies, the in-and-out defensive landing, forced deflection and material counts, and both dog-hole wins plus the escape after vacating its guard. The shared article verifier replays every scripted hop.
- **25 focused tests across three files passed**, plus diagram verification and the final production build. Independent editorial review passed at **94/100 with zero P0** after one citation wording correction. Local evidence is under `.generated/article-review/traps/`, with test/build logs named in [strategy-verification.md](../strategy-verification.md).
- The built page fits desktop and 390-pixel phone viewports. End/reset stepping, both exercise wins and the defensive promotion worked in the browser; no warnings/errors were captured. Temporary browser emulation, tab and preview were cleaned up. Local browser checks do not replace actual-device or production acceptance.
- Updated the content plan, strategy verification, launch readiness and changelog. No commit, deployment, production migration or outreach was performed.
- Remaining: the friends guide with three actual annotated screenshots, wider administration requirements, launch artifacts, puzzle publishing/runner and owner/production gates. The approved daily coin reward was already implemented; this confirmation did not add a second reward mechanism.

## Previous checkpoint: live result lifecycle and rematch restoration

The preceding reward confirmation rechecked existing behavior and did not advance the roadmap. This pass completes live adoption of the result repository: terminal handling, result actions, navigation release, reconnect and startup consume the same accepted viewer/consent state. The full roadmap remains active; owner browser and production acceptance are not established by local tests.

- Terminal checkpoint, settlement intent, game-claim release, result initialization and source-room closure commit atomically before publication. The closed room revision prevents delayed hydration from restoring an older room or a dismissed spectator.
- Player/spectator dismissal and the release preceding idle commands await repository actions. Rematch consent and one creation identity survive restart; colours swap, bot consent is automatic, and uncertain reservation responses reuse the accepted game. Human disconnection before installation cancels the uninstalled reservation and resets consent.
- Startup restores games, rooms and retained result viewers before listening. One outstanding consent survives restart. Accepted consent without a child resumes when humans reconnect; an uninstalled reserved child is cancelled with its parent-view restoration, while checkpointed children restore as active games. Spectators remain on the old result. Overdue results expire before sessions are recreated, using the original deadline.
- Reconnect waits across database commits not yet projected and reconciles result membership before its first snapshot. Opponent departure derives from persisted viewing flags. Result expiry and notices share a transaction; no separate in-memory rematch set or finished-room cleanup writer remains. Retired results cannot publish late settlement data, and shutdown still drains that work.
- **579 tests across 58 files passed**, and the production build passed (`.generated/tests-result-live-final.log`, `build-result-live-final.log`). Fourteen new integration cases cover repeated restoration, both rematch consents, bots, spectators, lost responses, uninstalled cancellation, due deadlines, late projection, reconnect commit gaps, shutdown and real Socket.IO dismissal across two fresh server processes. Repository tests now assert automatic terminal initialization and rollback; older expiry tests use the persisted deadline.
- Updated [result-lifecycle.md](../result-lifecycle.md), notice/room/navigation/deployment documentation, README, launch readiness and changelog. Migration `20260911090000_result_records` remains test-only; no commit, deployment, production migration or outreach was performed. Historical result views have no backfill, so drain or explicitly expire those old screens when rolling out.
- Next: complete the two remaining strategy guides and launch artifacts, resolve the broader administration requirements, and close puzzle publishing/runner and owner/production gates. Manual result acceptance is listed in the lifecycle document; local fault tests do not replace Railway continuity and target-capacity measurements.

## Previous checkpoint: result lifecycle repository and atomic rematch handoff

The preceding pass persisted notices and dismissal. This pass adds the owning result-state/viewer/consent boundary, including source-room transfer and rematch reservation/cancellation. **Live terminal/result handlers and startup restoration have not adopted these records yet.** Finished-result/rematch recovery remains incomplete; the full goal stays active.

- `GameResultRecord` stores explicit OPEN/STARTING/REMATCHED/CLOSED state, original result deadline, player viewing/consent, spectator context/deadlines and rematch identity. Pure domain actions govern dismissal, consent and expiry. Exact command receipts and revisions prevent duplicate/stale actions; repeating initialization cannot resurrect dismissed viewers. Bots consent automatically without an exclusive viewer claim.
- Initialization requires a terminal checkpoint and settlement intent. It can close the source room and transfer spectators into result claims atomically, with rollback of the entire handoff. A previously closed room without a result record is not accepted as evidence of its viewers. Terminal checkpoint transactions now follow room-before-game locking, ready for the initializer to join that owning transaction; they do not call it automatically yet. Late spectator admission to terminal games is rejected.
- `GameRun.resultSourceId` binds an accepted rematch. Parent-result/child-game/account lock ordering, one saved key/colour order and shared claim checks govern creation. Reservation swaps colours, creates a free game and transfers human claims in one transaction; repeated reservation returns that child. An uninstalled cancellation restores result viewers and resets consent, or expires them with notices if the original deadline passed. Started play cannot be undone. Shared room/game admission and guest retirement recognize result claims.
- **565 tests across 57 files passed**, plus production build (`.generated/tests-result-records-final.log`, `build-result-records-final.log`). Seventeen new PostgreSQL cases cover fixed deadlines, exact retries, transaction rollback, spectator transfer/expiry, closed-room inference refusal, consent races, rematch handoff/cancellation, expiry notices, bots, admission conflicts, guest protection and version/ownership checks. Existing Socket.IO and shutdown tests passed with the tightened lock order. These are repository/compatibility checks, not proof of live result-screen recovery.
- Migration `20260911090000_result_records` was applied only to the isolated test database; Prisma Client was regenerated. It adds empty result/viewer/receipt tables and a nullable source-result association, without backfilling old views. No commit, deployment, production migration or outreach was performed.
- Next: wire result initialization into terminal commit; replace live leave/expiry/rematch mutations and await durable release for navigation; restore result viewers/timers and pending rematches before listening; verify Socket.IO and separate-process behavior. [result-lifecycle.md](../result-lifecycle.md) specifies this remaining integration. No new browser check is requested before that candidate exists. Wider administration, two guides, launch artifacts, puzzle publishing/runner and owner/production gates also remain.

## Previous checkpoint: durable notices and exact-event dismissal

The preceding pass completed live room persistence and startup restoration. This pass persists removal/expiry notices and their dismissal, replacing the in-memory notice writer. Recorded notices survive process replacement; rematches and finished-result views, including their future expiry timers, remain unfinished. The full goal remains active.

- `SessionNotice` stores immutable user/effect identities, ordered events and dismissal tombstones. Repeated effects cannot duplicate, reorder or revive a notice. The latest event remains authoritative even if dismissed, so older undismissed messages do not reappear. Storage uses gameplay ownership and account locks; domain policies stay in `notices.js` and room departure rules, while sessions project accepted records with stale-read protection.
- Room removal/closure notices commit with roster, claims and command receipts. Voluntary leavers receive no forced-removal message; remaining spectators do when the final human leaves. Game completion does not produce a false missing-human room notice. Finished-game expiry persists its notices before releasing current viewers; storage failures retain the view and retry, and shutdown drains admitted writes without late publication.
- Reconnect loads the notice before its first snapshot. Dismissal targets only the authenticated user's exact event; an old dismissal cannot clear a newer message or another user's event. Lost responses retry safely. A sequence/dismissal cursor prevents a delayed pre-dismissal projection from reviving an event.
- **548 tests across 56 files passed**, and production build passed (`.generated/tests-session-notices-full.log`, `build-session-notices.log`). Twelve new tests cover atomic rollback, room closure policy, session reconstruction, retry identity, stale/cross-account dismissal, delayed projection, uncertain responses, result-expiry outages, voluntary leave, shutdown draining and two fresh server processes around a real socket dismissal. The process helper also retains the waiting-room first-snapshot check. The first focused attempt caught a test-file syntax error; it was corrected before the passing focused and full runs.
- Migration `20260911080000_session_notices` was applied only through isolated test setup, and Prisma Client was regenerated. It creates an empty event table and does not backfill old process memory. Deployment must apply it before the new binary. No commit, deployment, production migration or outreach was performed.
- Manual acceptance: have a host remove a player, restart and reconnect the removed player; confirm the explanation remains. Dismiss it, restart again and confirm it stays gone. See [session-notices.md](../session-notices.md). Remaining: durable rematches and finished-result views/timers, wider administration, two guides, launch artifacts, puzzle publishing/runner and owner/production gates. Notice persistence does not restore an expiry event that had not committed before a result-view runtime was lost.

## Previous checkpoint: live room persistence and startup restoration

The room repository now owns live creation, membership, readiness, start identity, departure and closure. Startup restores waiting rooms and active-game spectator roles before accepting connections. The full goal remains active: this closes the previously unwired room boundary, not the remaining notice/rematch/finished-result lifecycle or production gates.

- Live commands use serialized room transactions and durable request identities, then hydrate account metadata and project accepted records into sessions. Starts use the persisted key/colour order and source-room relation. A lost game/bot-start response confirms the original game automatically; departure confirms cancellation/refund before its own roster/claim/receipt commit. Existing wager members can still leave or update presence after their balance falls.
- Bootstrap validates room versions, canonical state and game association after game recovery; it resets starts without reservations, restores cancelled starts, closes terminal rooms and rebuilds player/spectator roles. Saved invitation codes generate fresh QR assets. Human readiness resets; bots remain ready. Already offline deadlines survive repeated restarts.
- One room-owned timer handles persisted waiting-player/spectator expiry and cleanup retries. Presence writes retry on storage failure. The old session-owned room timer is removed; active-game forfeiture keeps its separate game policy. Reconnect reads durable membership and waits for reconciliation before its first snapshot, sync response or command admission.
- **536 tests across 55 files passed**, plus production build (`.generated/tests-live-room-recovery-full.log`, `build-live-room-recovery.log`). Eleven new checks cover repeated restart/deadlines, private invitations/QR, bots, active-game spectators, uninstalled wager refunds, unsupported versions, lost room responses, presence retry, unaffordable existing members and a fresh server process's first snapshot. Existing socket tests retain refund/departure/reconnect races; their fixtures now explicitly stage durable uninstalled reservations instead of depending on the old lost-start-response bug.
- Manual acceptance: restart a private waiting room with a spectator, reconnect through the same invite, and verify URL/role agreement and fresh human readiness. Repeat with a bot; its readiness must remain automatic. Leave everyone disconnected for two minutes across another restart and confirm cleanup. For an active game, both humans Resume game and the spectator sees the same board. See [durable-rooms.md](../durable-rooms.md).
- No new migration was needed beyond the existing test-only `20260911070000_durable_rooms`; production rollout must apply it before this binary and account for old in-memory rooms that cannot be reconstructed. No commit, deployment, production migration or outreach was performed. Remaining: durable notices/dismissal, rematches and finished-result views, wider administration, two guides, launch artifacts, puzzle publishing/runner and owner/production gates.

## Previous checkpoint: durable room repository and atomic game handoff

The preceding pass closed shutdown races. This pass adds the persistence boundary needed for room recovery: `RoomRecord`, exclusive human `ActiveRoomMember` claims, command receipts and nullable `GameRun.roomId`. **Live room handlers and startup restoration are not wired to this repository yet.** The full goal remains active; repository tests do not mean browser room recovery works.

- Durable state has WAITING/STARTING/PLAYING/CLOSED transitions, canonical fields and version/revision checks. Database identity supplies account/bot flags; bots stay ready without exclusive claims. Creation retries retain their room/code, command retries return current accepted state, closed invites are not recycled and unknown versions fail for review. Existing room actions and the SSR invite page share settings/code validators.
- Room and game admission share account locks and check each other's claims. Room-backed game creation validates its accepted key/colours/settings/mode, then commits association, claim transfer, run and stakes together. Cancellation restores waiting-room claims/readiness and refunds atomically. It retries its standalone transaction if a concurrent creation reveals a previously unknown source-room association. Guest retirement protects durable room claims without local sessions.
- **525 tests across 54 files passed**, production build and whitespace checks passed (`.generated/tests-room-records-full.log`, `build-room-records.log`, `check-room-records.log`). Fifteen new PostgreSQL tests cover races, retries, handoff/rollback/cancellation, readiness, bots, guest retention and ownership/schema fencing. Focused invite checks passed after the final validator import. Migration `20260911070000_durable_rooms` was applied only to the isolated test database; the Prisma client was regenerated.
- Next: adopt the boundary in live room commands/publication, then restore rooms, roles and deadlines at startup. [durable-rooms.md](../durable-rooms.md) records the transaction contract and remaining integration. Durable notices/results/rematches, broader administration, two guides, launch artifacts and production/owner gates also remain. No commit, deployment, production migration or outreach was performed.

## Previous checkpoint: shutdown admission and asynchronous continuation safety

The preceding pass completed launch-preparation documentation. Inspection of the remaining room lifecycle found a prerequisite race: shutdown drained installed games and departures but not pending room creation or game starts. A delayed account/QR/transaction response could install state after shutdown began. This pass closes that race; durable room restoration remains unfinished.

- One owner-bound runtime lifetime tracks domain commands, room setup, direct/quick-play starts and optional move analysis. Both shutdown paths close admission synchronously and drain registered work. Captured continuations cannot adopt a fresh ownership token after an await; the database owner fence still governs persisted writes. The helper contains scheduling/lifetime behavior, with business decisions retained in the domain.
- Game creation includes lifetime validity in its existing context check after reservation and the reveal checkpoint. The original owner refunds a confirmed uninstalled start once and releases claims before completion. An obsolete owner cannot refund it; the replacement uses the existing durable recovery rules. Room-start failures do not readvertise stopped rooms, and late matchmaking/rematch setup cannot create new work.
- Optional analysis and its bot-reaction chain are drained and reject stale results. Settlement name lookups cannot rearm a stopped game's result timer, and transport shutdown does not create disconnect chat for stopped games. Details and manual checks: [gameplay-shutdown.md](../gameplay-shutdown.md).
- **510 tests across 53 files passed**, the production build passed, and whitespace checks passed (`.generated/tests-gameplay-shutdown-full.log`, `build-gameplay-shutdown.log`, `check-gameplay-shutdown.log`). Thirteen new tests pause real operations around QR generation, account lookup, reservation/reveal commits, ownership replacement, whole room starts, matchmaking, nested work, analysis, settlement lookup and disconnect handling. Two older domain fixtures now acquire ownership as application bootstrap does. The earlier full-suite failure identified that missing fixture setup; it was corrected before the final passing run.
- No browser retest is required for unchanged UI in this pass. The local/staging manual check is stopping during room/bot setup and verifying that reconnect resolves the accepted game/refund without the old process advertising a new game. A hard kill or host shutdown deadline still relies on durable records; graceful draining alone is not Railway continuity evidence.
- Remaining: durable waiting-room/spectator/rematch/notice/finished-result lifecycle, wider administration, two strategy guides, README screenshot/clean setup, target-capacity and production acceptance, puzzle publishing/runner and owner/external gates. No commit, deployment, production migration or outreach was performed. The full goal remains active.

## Previous checkpoint: launch preparation and claim corrections

The preceding turn recorded the approved daily puzzle reward in plan 03. This pass reconciles launch material against the worktree and adds [launch-readiness.md](launch-readiness.md) as the current release gate record. Production readiness remains unproven; the full roadmap stays active.

- Plan 05 now has three distinct community review drafts, an outreach template and two directory blurbs that describe Pure's actual rules. Removed English-tournament, fixed-depth, unverified hosting-price and universal recovery claims. HN now has an author brief only: current official guidelines prohibit generated or AI-edited posts, so the owner must write that venue's prose independently.
- The release record separates local implementation from deployed verification and names required evidence for first-turn/reconnection behavior, URL agreement, replacement, capacity, puzzle publishing, discovery, previews, feedback, licence and analytics. It preserves the full roadmap requirement rather than calling unfinished work a launch deferral. Five manual acceptance groups organize the owner checks.
- README now states recovery limits and stops advertising an unverified feedback destination. Deployment notes include the guest-retirement migration and current account behavior. The overview and links log identify historical baseline observations and unsent research candidates; no outreach dates or traffic results were fabricated. The calendar no longer points an unrelated dev.to article's canonical at the changelog, and federation outreach requires a ruleset fit check.
- Documentation verification checks internal links/heading targets, required draft sections and referenced implementation paths (`.generated/verify-launch-docs.mjs`, `.generated/verify-launch-docs.log`). No runtime code changed, so the application suite and build were not rerun for this documentation pass. Previous test/build results retain their original scope.
- Remaining: two strategy guides, durable waiting-room/spectator/rematch/notice/finished-result lifecycle, wider administration, a current README gameplay screenshot, clean-setup verification, target-capacity and real Railway checks, puzzle publication/runner and owner/external gates. No commit, deployment, production migration, email or community submission was performed.

## Previous checkpoint: beginner opening guide and verified recapture choices

The preceding turn completed browser account-upgrade recovery. This pass adds the tenth of twelve strategy guides: a 1,989-word beginner-opening lesson with five six-move boards, four FAQ answers, sourced opening names and explicit Pure/English rule boundaries. The existing first-move guide links to it and corrects its claim that 28x19 was the only recapture after 15x24; 27x20 is legal too.

- Independent editorial review passed at **93/100 aggregate, zero P0**, covering source/rendered agreement, names, explanations, word count, metadata and diagrams (`.generated/article-review/openings/`). The shared content manifest supplies hub, related navigation, metadata and sitemap integration.
- **21 tests across three files passed**, as did the production build and whitespace check (`.generated/tests-opening-guide.log`, `build-opening-guide.log`, `check-opening-guide.log`). The opening test verifies all five material counts, alternative recaptures, the Cross's blocked landing, Double Corner's supporting square and Bristol's legal continuation. All ten guides passed the scripted-diagram verifier.
- Built-page browser checks passed at desktop and phone width, including end/reset controls and accessible step announcements. A shared CSS correction prevents move notation wrapping beside long explanatory notes. The preview process/tab were closed and the viewport reset.
- Manual acceptance: open `/strategy/checkers-openings-for-beginners`, step through an exchange and reset; confirm the move labels, board and explanatory notes agree on a phone.
- Remaining content: `checkers-traps` and `play-checkers-online-with-friends` with three actual annotated screenshots. Durable room/spectator/rematch/notice/finished-result lifecycle, wider administration, target-concurrency/Railway acceptance, launch preparation and owner/external gates remain open. No commit, deployment, production migration or outreach was performed; the full roadmap remains active.

## Previous checkpoint: account-upgrade confirmation and browser recovery

The preceding turn fixed cancelled-result interpretation. This pass closes the browser's uncertain-upgrade path using the existing account transition and authoritative `/auth/me` read, without introducing another financial receipt or persisting credentials.

- The upgrade controller checks registration before submitting, confirms after an uncertain/error response, and distinguishes an unresolved outcome from a validated rejection. The sheet offers a read-only status check. Duplicate client calls coalesce; captured tokens, account checks, disposal and 15-second request bounds protect against stale responses and indefinite saving states. Renewed guest tokens are adopted before the subsequent save.
- `/auth/me` replaces outdated guest/name claims and repairs live identity through the existing shared identity service. Reload bootstrap uses this same read path. A conversion committed before the response or live publication can recover without restarting the socket or minting another starter grant. See [account-lifecycle.md](../account-lifecycle.md).
- **496 tests across 52 files passed**, production build and whitespace checks passed (`.generated/tests-upgrade-recovery-full.log`, `build-upgrade-recovery.log`, `check-upgrade-recovery.log`). Fifteen controller cases cover lost responses, previous/concurrent conversion, validation versus uncertainty, status checks, token renewal, overlapping saves, account replacement at all three request phases, malformed responses, timeout and disposal. Auth integration also checks fresh registered-token issuance and commit-before-publication repair; existing socket tests cover upgrade in a waiting room.
- Manual acceptance: save a guest while waiting in a room and verify that play stays mounted, the account becomes registered and the starter grant appears once. Reload to confirm persistence. If a save reports uncertainty, Check saved account should recover once the connection returns; retrying must not add another grant.
- The broader roadmap remains active: durable room/spectator/rematch/notice/finished-result lifecycle, wider account administration, target-concurrency and real Railway acceptance, three guides, launch preparation and owner/external gates. No commit, deployment, production migration or outreach was performed.

## Previous checkpoint: consistent cancelled-game results

The preceding turn recorded reward approval and account verification. This pass fixes a result inconsistency exposed by restart cancellation: private history and profiles treated `ABORTED` as a loss, while the replay board presented a draw with win styling.

- Shared result interpretation now supplies winner identity, participant outcome and neutral labels across history, profiles, live results, replay pages/boards, discovery and preview cards. Cancelled games remain accessible through history but excluded from discovery. Unknown codes do not become losses or draws; live results prefer the persisted result code. See [game-restoration.md](../game-restoration.md).
- **480 tests across 51 files passed**, production build and whitespace checks passed (`.generated/tests-game-results-full.log`, `build-game-results.log`, `check-game-results.log`). Eleven new checks include both authenticated histories, actual API data rendered through real profile/replay components, winner styling and preview/share text. The first full run exposed a timing-dependent publication-failure fixture; that test now explicitly uses the queued job's saved due time, then the full run passed.
- Manual acceptance: open a cancelled recovery game from history and its replay. Confirm Cancelled/Game cancelled, neutral styling, and the refund explanation; ordinary wins, losses and draws should retain their normal labels.
- The full roadmap remains active. Durable waiting-room/spectator/rematch/notice/finished-result lifecycle, wider account recovery, target-concurrency and Railway acceptance, three guides, launch preparation and owner/external gates remain open. No commit, production migration, deployment or outreach was performed.

## Previous checkpoint: guest retirement and account identity

The account lifecycle pass replaces hard deletion with explicit retirement, preserving historical references and protecting active sessions, open games, pending settlement/payouts, balances and inventory. Renewal, upgrade and retirement share account locks; cleanup is bounded, drains on shutdown and is fenced to the captured gameplay owner. HTTP/socket identity comes from the current database account. Registration and upgrade commit one starter grant with its ledger entry; waiting-room registration flags update after upgrade.

- Full suite: **468 tests across 50 files passed**; production build passed (`.generated/tests-account-lifecycle-full.log`, `build-account-lifecycle.log`). The subsequent cleanup ownership refinement passed **39 tests across three files** (`tests-account-lifecycle-fencing.log`). See [account-lifecycle.md](../account-lifecycle.md) for migration, policy, coverage and limits.
- The owner confirmed the one-coin daily puzzle reward. The existing implementation awards registered humans once for an eligible unrevealed solve of today's UTC puzzle, with no archive reward; attempt, balance and ledger commit together. Retry/concurrency coverage remains in the puzzle tests.
- Still open: wider account administration and uncertain-upgrade client recovery; durable waiting-room/join-code/readiness/session/notice/spectator/rematch/finished-result lifecycle; target-concurrency and Railway replacement acceptance; three strategy guides, launch preparation, owner decisions and external gates. This is progress, not roadmap completion. No commit, deployment, production migration or outreach was performed.

## Previous checkpoint: confirmed room departure and failed-start refunds

The preceding turn made progress on durable game membership. This pass resolves failed-start reservations before room membership is discarded. The full goal remains active; no commit, deployment, production migration or outreach was performed.

- Leave, kick, room expiry and failed bot-room cleanup await the same departure boundary. A player change reserves the room as updating; cancellation/refunds and claim release are confirmed before roster, host, notice or session/navigation changes are published. Spectator departure never cancels player stakes.
- The existing cancellation repository now distinguishes confirmed absence from a guard declining cancellation, checks current context after lock waits and confirms an already committed abort. Failed cancellation retains the room/attempt for retry; a lost response cannot issue another refund.
- Room/game expiry share one retry scheduler. A returning player is checked before cancellation and before departure publication. If the refund already committed during reconnect, the player remains in the room with a cleared attempt and fresh readiness. Busy/storage failures retry while the same disconnected context remains valid.
- Room screens, banner and directory display pending changes; conflicting ready/join/start operations cannot run through a pending cancellation. Shutdown stops rooms and drains pending membership-change promises. This does not claim that every in-flight room operation or lifecycle state is durable.
- **447 tests across 49 files passed**, and the production build passed (`.generated/tests-room-departure-full.log`, `build-room-departure.log`). Nine new socket checks cover failed/uncommitted starts, commit-before-departure, cancellation outages/response loss, kicks, spectators, both reconnect timings and expiry retry. The earlier bot response-loss check now verifies immediate failed-room cancellation. See [room-departure.md](../room-departure.md).
- Still open: durable room/join-code/readiness/session/notice/spectator/rematch/finished-result lifecycle and restart restoration; account cleanup; target-concurrency and real Railway replacement acceptance; three strategy guides, launch preparation, owner decisions and external gates. Financial cancellation and room-memory publication are ordered, not a durable room transaction.

## Previous checkpoint: durable human game membership

The preceding turn made progress on startup game restoration. This pass enforces active-human membership at creation and release boundaries. The full goal remains active; no commit, deployment, production migration or outreach was performed.

- GameRun/checkpoint remains the source for participation; a primary key on `ActiveGamePlayer.userId` enforces one unfinished game per human across colours, opponents and racing transactions. Guests claim seats too; bots can serve multiple games. Creation, stakes and claims commit together under the existing ownership/account locks.
- Terminal checkpoint/job commits release claims before financial settlement completes. Cancellation refunds/releases atomically. Old settlement, abort and creation retries are scoped to their game and cannot erase or resurrect a newer membership.
- Startup rebuilds claims from the validated unfinished checkpoint roster before listening, removes stale terminal/nonparticipant claims and remains ownership-fenced. Migration creates constraints without guessing historical membership in SQL. Conflicting saved rosters still require review.
- Runtime creation cancels a known uninstalled prior attempt through the existing guarded cancellation service before another start. Socket coverage verifies a bot creation response lost after commit, followed by a successful retry with one active claim and a cancelled orphan run. Started/terminal play is never reset by this cleanup.
- **438 tests across 49 files passed**, and the production build passed (`.generated/tests-membership-full-final.log`, `build-membership.log`). Eight PostgreSQL membership tests include primary-key enforcement, competing starts, creator process exit, bot concurrency, rollback, delayed settlement and startup/fencing behavior. The initial broader run found test bot-key collisions; restoration fixtures now use the app's registry lookup. See [game-membership.md](../game-membership.md).
- Still open: immediate cancellation/refund on abandoned waiting-room closure, durable room/spectator/rematch/notice/finished-result lifecycle, account cleanup, target-concurrency/real Railway replacement acceptance, three strategy guides, launch preparation, owner decisions and external gates. An active-game claim is not presence or a finished-result session; this checkpoint does not close the wider lifecycle roadmap.

## Previous checkpoint: startup restoration and explicit return to play

The preceding reward-confirmation turn rechecked existing behavior and added no implementation. This pass implements unfinished-game restoration after ownership acquisition. The full goal remains active; no commit, deployment, production migration or outreach was performed.

- Startup validates open runs, restores the latest committed game and reconstructs its human game sessions before listening. Saved turn, board/capture/draw history, clocks, start time and pending draw decisions survive. Runtime construction is shared with normal creation.
- A committed two-minute recovery pause is distinct from colour reveal. The browser displays the saved board and a Resume game action. Readiness is bound to the current game, ownership generation and connection; replaced connections and subsequent restarts cannot reuse it. Human readiness resumes clocks and bot work; interrupted colour reveals do not replay.
- At the deadline, one returning human receives a disconnect win. With no returning human, cancellation refunds wagers once and changes no ratings, statistics or reward eligibility. Bots never win through absent-human recovery. Terminal state/job and later financial receipts retain their established atomic boundaries; failed deadline writes retry.
- `recoveryVersion` defaults to zero even for old writers after migration; only new creation code explicitly writes version one. A version-one null/revision-zero run is safely cancelled as an uninstalled start. Legacy null checkpoints, overlapping saved human memberships and inconsistent terminal state stop startup for review before side effects.
- **427 tests across 48 files passed.** Two later capture-chain/deadline-retry checks brought the focused restoration suite to **15 passing tests**; the final production build passed. Actual fresh-server tests reconnect authenticated sockets, restore the saved turn/clocks, resume humans and receive a bot's move without another sync request, then exit through ownership-loss shutdown. Evidence: `.generated/tests-restoration-full.log`, `tests-restoration-final.log`, `build-restoration-final.log`; contract and manual checks in [game-restoration.md](../game-restoration.md).
- Still open: durable active-human membership uniqueness at creation; waiting-room, spectator, rematch, notice and finished-result lifecycle restoration; account cleanup; target-concurrency/real Railway replacement acceptance; three strategy guides, launch preparation, owner decisions and external gates. Older clients require rollout handling for the new recovery control. This is unfinished-game recovery, not complete lifecycle or seamless failover certification.

## Previous checkpoint: gameplay writer ownership fencing

The previous turn made progress on committed transitions. This pass adds transactional writer generations and ownership-loss shutdown. The full goal remains active; no commit, deployment, production migration or outreach was performed.

- Startup claims a PostgreSQL ownership generation. Creation, failed-start cancellation and gameplay transition transactions hold a shared ownership lock until commit/rollback; takeover waits for those writes, then fences the old generation. There is no unfenced gameplay-service fallback.
- GameRoom and in-flight creation capture their ownership identity. Old aggregates cannot adopt newer checkpoints and keep writing. Reconciliation reads are fenced. Immutable terminal jobs can still finish their already accepted outcome through the settlement receipt contract.
- A bounded watcher detects takeover while idle. Ownership loss stops runtime work and closes transports; SIGTERM/SIGINT share the cleanup path. Single-replica operation remains required. Automatic game/session restoration and zero-downtime handoff are not claimed.
- **413 tests across 47 files passed**, and the production build passed. A subsequent sixth ownership test booted the actual server, verified HTTP readiness, changed ownership and observed clean exit; all six ownership tests passed. PostgreSQL lock-wait inspection and separate-process takeover supplement socket checks that an old aggregate cannot adopt/write the new revision. See [gameplay-ownership.md](../gameplay-ownership.md) and `.generated/tests-fencing-full.log`, `tests-fencing-bootstrap.log`, `build-fencing.log`.
- Still open: restore the latest aggregate and sessions/routes/spectators at startup, resolve null checkpoints/open starts safely, persist room/rematch/notice lifecycle, define downtime clocks/readiness and validate target concurrency/real rolling deployment. Account cleanup, three strategy guides, launch preparation, owner decisions and external acceptance gates remain.

## Previous checkpoint: committed gameplay checkpoints and command receipts

The previous goal turn made progress on creation/reservations. This pass puts accepted gameplay behind one commit-before-publish boundary. The full goal remains active; no commit, deployment, production migration or outreach was performed.

- GameRoom transitions use isolated drafts and the shared engine. The repository checks the durable command identity and expected revision, then commits checkpoint, command receipt and any terminal settlement job together. Only committed state is installed and published. A sync request cannot see a speculative board.
- Checkpoints retain engine history/capture/draw state, clocks, reveal state, start/end fields and draw offers/cooldowns. Human and bot moves, reveal transitions, clock/explicit endings and draw decisions use the boundary. Room/session/rematch/spectator lifecycle persistence remains separate unfinished work.
- Uncertain commit responses consult durable receipts/checkpoints without rerunning accepted moves. Bot and reveal persistence failures retry; disconnect forfeiture checks for reconnect before transition evaluation. Invalid player commands do not reset elapsed clocks.
- The earlier unqueued-terminal memory buffer and enqueue retry hook were removed. Terminal state and its job now commit before publication; a failed terminal commit leaves the last confirmed live game.
- Six PostgreSQL transition tests include competing revisions, atomic checkpoint/receipt/job rollback, exact capture-history restoration and a separate process exiting after commit. Seven socket checks cover snapshot isolation, rollback, response/cache loss, invalid-command clocks, terminal failure, first-bot-move retry and reconnect versus queued forfeiture. **407 tests across 46 files passed**, and the production build passed (`.generated/tests-checkpoints-full.log`, `build-checkpoints.log`). See [game-checkpoints.md](../game-checkpoints.md).
- Still open: automatic loading/restoration of the latest aggregate, distinguishing legacy null checkpoints from newly uninstalled runs, ownership fencing, session/room/route/spectator persistence, downtime clock/readiness policy and target-concurrency validation. Account cleanup, three strategy guides, launch preparation, owner decisions and external acceptance gates remain. Checkpoint persistence does not by itself prove complete restart continuity.

## Previous checkpoint: durable creation and wager reservation

The previous goal turn made progress on terminal recovery. This pass moves creation and stakes into one transaction and verifies failure/retry boundaries. The full goal remains active. No commit, deployment, production database change or outreach was performed.

- GameRun persists the creation key, database-assigned navigation ID, participants, configuration, initial engine state and reservation outcome. Both conditional stake debits and ledger entries commit with creation. Retrying an identity returns the same record without another debit.
- Room start retains its key/colour assignment across an uncertain creation response. Human start reservations close the same-process async race; session/context revalidation prevents installation into an unrelated session. Failed installation refunds and ABORTED status commit together.
- Settlement requires a matching durable wager reservation and closes it in the same transaction as replay, statistics and every financial effect. The earlier room-layer stake/refund transactions are removed. Existing economy tests now fund their wager pots before settling them.
- Ten real PostgreSQL creation tests include a separate process exiting after reservation commit and a subsequent same-key confirmation, rollback at creation/abort/settlement boundaries, reservation validation and concurrent spending. Three socket tests cover lost creation response, competing human starts and context change after commit. The full suite passed **394 tests across 45 files**, and the production build passed (`.generated/tests-reservations-full.log`, `build-reservations.log`). See [game-creation.md](../game-creation.md).
- Still open: persist accepted transitions/command identities, recover open/uninstalled runs automatically, derive terminal jobs from durable live state, fence overlapping owners, restore sessions/routes/spectators and define downtime clocks/readiness. InitialState is only the initial board, never a restart checkpoint for accepted play. Account cleanup, three strategy guides, launch preparation, owner decisions and external acceptance gates also remain. This checkpoint does not claim full restart continuity.

## Previous checkpoint: durable terminal settlement recovery

The previous goal turn made progress on purchases/transfers. This pass implements durable terminal settlement jobs and controlled process-exit verification. The full goal remains active; no commit, deployment, production database change or outreach was performed.

- GameRoom captures its terminal intent once and commits a job before settlement. Job completion, replay, statistics and every financial effect share the existing settlement transaction. Complete normalized intent identity prevents changed retries.
- A bounded startup/background worker processes due jobs sequentially, coalesces timer calls, backs off failed jobs and continues with other work. Participant references prevent deletion from orphaning a queued result.
- Failed result screens receive recovered receipts through the existing authoritative snapshot and one replay announcement. An intent that cannot yet be enqueued is retained in memory for retry; its loss on a process crash before enqueue remains explicitly unsolved.
- Nine PostgreSQL recovery tests include separate processes exiting after enqueue and after settlement commit, followed by fresh-process recovery. The socket test covers database interruption before enqueue and subsequent result recovery. The focused run passed 64 tests; the full suite passed **381 tests across 44 files**, and the production build passed. See [settlement-recovery.md](../settlement-recovery.md) and `.generated/tests-recovery-full.log` / `build-recovery.log`.
- Still open: atomic game creation/wager reservation, persisted accepted transitions/command identities, deriving terminal intent from committed live state, server fencing, restoring game/session/route/clock/spectator state, guest cleanup and other audit paths. Three strategy guides, launch preparation, owner decisions and external acceptance gates also remain. Terminal-job recovery does not establish live-game restart recovery.

## Previous checkpoint: purchase and transfer recovery

The full goal remains active. This pass closes the identified purchase/tip transaction gaps and adds shared browser confirmation. No commit, deployment, production write or outreach was performed.

- Purchases and tips now delegate to one wallet-actions service, with ordered account locks, conditional debits and a durable owner-scoped request receipt. Inventory, vault allocation, both transfer legs and their receipt commit or roll back together. Equipment choices are serialized per account.
- New purchase burn totals are allocation metadata rather than a second wallet debit. Treasury includes historical burn metadata and new receipt allocations without double counting.
- Shop and Friends share one account-scoped payment journal and confirmation panel. A request ID is saved before sending; uncertainty retains it across navigation/reload in the same tab. Deadlines, repeat-input blocking, account generation guards and a current-wallet refresh keep retries separate from new payments. Closed-tab/cross-device recovery is not claimed.
- The full suite passed 371 tests across 43 files and the production build passed. Final API-body validation/recovery-copy edits passed 46 focused tests across four files. See [wallet-actions.md](../wallet-actions.md) for evidence, migration/legacy-client behavior, limitations and three manual acceptance checks.
- Still open: durable live games/commands, atomic wager reservation, restart recovery, server fencing, account cleanup/other economy audit paths, three strategy guides, launch preparation, unanswered owner decisions and external acceptance gates. This is progress, not completion.

## Previous checkpoint: atomic game settlement and vault claims

The full goal remains active. The previous goal turn was progress; this pass continues the financial/restart audit. No commit, deployment, production database write or outreach was performed.

- Replaced GameRoom's separate replay/statistics/reward operations with one settlement service and transaction. Each aggregate supplies a UUID key; the saved Game row retains a unique key and durable receipt. Concurrent retries cannot repeat ratings or payment. The game is marked saved only after the complete transaction commits.
- Wallet/vault debits are conditional; shared helpers can join an owning transaction. Vault initialization now has one constrained identity. The migration preserves the sum of any existing duplicate vault rows.
- Daily bounty eligibility is consumed even when payment is queued, using UTC. Milestone eligibility, credits/obligations and peak rating are atomic. Claimed payout rows retain a receipt so a repeat owner request after a lost response confirms success without another credit; pending totals exclude those receipts.
- Result payout totals now include refunds, persisted start/end times are accurate, wager eligibility uses end time rather than retry time, and bot-win caps are serialized through the database.
- Eleven real PostgreSQL tests exercise duplicates, concurrency, rollback and retry. The full suite passed 347 tests across 41 files and the production build passed. Final receipt/grant-time refinements received a focused follow-up run; see docs/game-settlement.md for evidence and limits.
- Still open: durable live-game/command persistence, wager reservation and startup recovery, fencing overlapping servers, and full crash/reconnect tests. The subsequent wallet-actions checkpoint above addresses purchase/tip atomicity and same-tab client recovery; broader economy/client recovery remains under audit. These are not solved merely by atomic terminal settlement. Three strategy guides, launch preparation, owner decisions and external acceptance gates also remain.

## Previous checkpoint: verified endgames, comparisons and exact scoring

The goal remains active. Nine of twelve English strategy guides now exist. Beginner openings, traps and the friends guide (with actual annotated screenshots) remain. Nothing has been committed, deployed, imported into production or posted externally in this pass.

- Added the two-kings endgame, computer-opponent and checkers-versus-draughts guides. The comparison uses federation/association sources and an original 10×10 illustration. It explicitly distinguishes Pure's promotion and king-landing rules from pool checkers.
- Replaced the roadmap's unsupported two-kings draw premise with a specific forced win. An independently replayed certificate covers all 15 terminal defensive branches; it is limited to the stated position, side to move and fresh draw history.
- Bot evaluation now sums exact integer twentieths before converting to its public score scale. Equivalent positions no longer acquire different scores through floating-point accumulation. Move-grading differences use the same units. The recorded opening experiment was regenerated, including the true three-way tie after 9-14 and the computer guide's depth comparison.
- Generator version 1.2.1 records the arithmetic change. Its deterministic seed fixture was updated without changing the unrelated API fixture. All 31 existing generator-1.2.0 launch puzzles passed independent deep re-verification with the corrected evaluator; no rows were written.
- The full suite passed 330 tests across 39 files after the arithmetic/fixture changes. The nine-guide build and diagram verification passed. The two endgame/computer guides passed independent editorial review at 93/100 aggregate; the comparison passed at 93/100, with no P0 findings. These are local/editorial checks, not production acceptance.
- Desktop and phone browser checks confirmed the comparison illustrations load, use the correct counts and stack without document overflow. See docs/strategy-verification.md for scope and remaining manual checks.
- Dependency updates reduced the local audit from 25 affected entries to three entries from one remaining Prisma configuration dependency. Its exposure and follow-up are recorded in docs/dependency-audit.md. The full 330-test suite and production build passed again afterward, and built metadata/schema/article links were checked for all nine guides.
- Deployment documentation now explicitly states that live games/sessions are process-local and requires a single replica. Same-server reconnect recovery does not establish survival of a server restart. Durable recovery and interrupted settlement remain open implementation/audit work.
- Public rate limits now share one client-address resolver with direct and explicit Railway modes, canonical IP identities, and bounded client/overflow buckets. The 37 relevant address, room, puzzle, bot-account and preview tests passed. Railway configuration and real-edge verification remain deployment steps; see docs/client-address.md.
- Launch preparation, outstanding owner decisions and the broader reliability/settlement audit remain open. Completion of these articles does not close the full roadmap.

## Previous checkpoint: strategy examples and editorial correction

The complete goal remains active. Six of twelve English strategy guides now exist; six remain to be written. No commit, deployment, puzzle production import or external outreach has been performed.

- Added backward-capture and double-jump guides. Revised the four original guides to correct the king-chain explanation, official draw conditions, captured-piece removal differences, board geometry and unsupported outcome claims.
- Replaced the unsupported bot experiment with a reproducible run and downloadable record containing settings, source hashes and complete reply scores. Live time-bounded behavior is explicitly distinguished from the recorded experiment.
- Added an AST-based diagram verifier and targeted checks of forced replies, supporting guards, promotion, king destinations and complete chains. Shared diagrams expand compound captures into visible hops and announce targets/current steps. ArticleLayout owns both Play links and uses the shared analytics helper.
- The production build and 23 relevant tests across five files passed. Built HTML metadata/schema/article links were checked for all six guides. A representative built page was exercised in the browser at desktop and phone width, including capture stepping and target announcements. See docs/strategy-verification.md for evidence limits and manual checks.
- Remaining content: checkers-versus-draughts, beginner openings, traps, two-kings endgames, beating the computer and playing with friends (including actual annotated screenshots). The original endgame outline contains an unverified draw premise and must be corrected from primary evidence.
- Launch preparation, unanswered owner decisions, broader reliability/settlement coverage and manual/external acceptance gates remain open. Passing content checks does not close those requirements.

## Previous checkpoint: gameplay feedback and recovery

The full roadmap remains incomplete. No commit, deployment or external posting has been performed.

- Both chat surfaces now use one acknowledged client/service contract, durable message IDs, safe retry, persisted mention metadata, generation-scoped history and text rendering. Real PostgreSQL and socket tests cover duplicate sends and reconnect recovery. See docs/chat-recovery.md.
- Room browsing and the tab count share one acknowledged, versioned directory. Filters are local projections, delayed reads cannot revive removed rooms, and loading/error/empty are distinct.
- Snapshots include the opponent's reconnect deadline, bot identity and connected spectator count. Spectator names survive room cleanup. Forced removal/expiry notices survive reconnect and use the existing command protocol for dismissal.
- Immediate offline/busy command failures publish feedback. Draw responses show pending state and disable repeat input. Resign/draw confirmations, room creation, account saving and side panels share native modal focus handling. The canvas board has a keyboard-accessible text/button overlay; puzzle keyboard navigation shares the same orientation helper.
- The full suite passed 313 tests across 35 files before the final accessibility edits; the subsequent focused accessibility/navigation/bootstrap/puzzle checks passed, and the production build completed without the previous Svelte warnings. Built-page HTTP smoke checks were rerun successfully. Browser keyboard/focus and visual acceptance remain with the owner.
- Strategy diagrams now cancel pending replies on reset/navigation and use browser workers through the shared scheduling core. The 59 relevant Node pool/search/preview/socket tests and two browser-adapter tests passed; the production build passed again. Manual diagram acceptance and editorial verification remain open.
- Still open: eight remaining articles, launch preparation, owner decisions, and the manual/external acceptance gates. The broader persistence/restart and reward-settlement audit is not proven by these tests.

## Previous checkpoint: puzzles and sharing

The full roadmap remains incomplete. The puzzle and sharing implementation is now present; this section supersedes the earlier discovery/worker checkpoint below. Nothing has been committed, deployed or posted externally.

- The puzzle API, SSR hub/date pages, interactive board, archive, UTC streaks and account-scoped progress journal are integrated. Full solve proof is checked by the server. The owner approved the one-coin daily reward; concurrent retries are covered by transactional attempt/wallet/ledger tests.
- Generator 1.2.0 stops at a settled tactical payoff and describes actual final material. The checked-in 31-puzzle launch artifact follows every difficulty slot (9 easy, 13 medium, 9 hard) and passed independent deep verification plus actual-engine replay. It has not been imported into production; owner visual/editorial acceptance and the operational publishing runner remain pending.
- Sharing uses one helper/component. Game, player, puzzle and invitation PNGs use shared templates, bounded memory, request coalescing and the existing worker-pool abstraction. Profile visibility is enforced in profile data, discovery links and cached image responses. New games persist end reasons; replay/profile navigation resets local view state.
- Full suite: 290 tests across 32 files passed. After adding persisted end reasons, 56 relevant tests passed and the production build passed. A built-adapter HTTP smoke check verified SSR puzzle boards, archived explanation visibility, hidden future/private pages, one generated preview tag per page and a valid puzzle PNG. The later full suite and built-page smoke checks cover the subsequent statistics/presentation edits.
- See docs/puzzles.md and docs/share-previews.md for boundaries, operations and manual checks. External social-card/crawler checks, Railway binary/latency checks and production publishing remain separate.

Still required: revalidate and resolve the remaining player-experience findings; complete the eight remaining strategy articles and editorial/diagram checks; finish launch preparation and owner decisions. The roadmap is not complete merely because these feature tests pass.


## Latest completed work: discovery, notation and background search

The full roadmap remains incomplete. This section supersedes the earlier integration handoff and review snapshots below. Nothing has been committed, deployed or posted externally.

- One server policy now drives replay indexability, the public games list and sitemap: no bots, no aborted games, at least one registered human, at least ten recorded moves. Display names do not determine account type.
- The dynamic sitemap includes static pages, all four existing article URLs, eligible players and games. It caches for ten minutes, coalesces generation, scans records with keyset pagination and splits at 40,000 URLs. Article metadata comes from a build-generated manifest using the same mdsvex compiler as pages. The old static sitemap is removed.
- Public `/leaderboard` and `/games` pages render on the server, have empty/error states and are linked from marketing navigation; the home rating feature links to the leaderboard.
- Replays, article diagrams and puzzle generation share `shared/notation.js`. The 11-15 opening and capture chains are verified against the engine.
- Bot search and optional move grading run through one bounded worker service, with time/queue limits, priorities, cancellation and crash recovery. The server validates game/turn/ply after worker completion. Search checks all capture continuations. See `docs/bot-workers.md`.
- Changelog and deployment notes are updated. Remaining older player-experience findings are not marked closed.

Verification: the full suite passed 246 tests across 24 files. The production build passed. A local HTTP smoke check against the built adapter returned 200 and server-rendered headings for both discovery pages and an article; `/sitemap.xml` returned the article URLs and parsed successfully as XML with the PowerShell XML parser. Worker tests include CPU responsiveness, saturation, cancellation, crash/timeout replacement and a socket-level delayed-result-after-resignation case. These are local checks, not production load or crawler validation.

Next: validate the puzzle generator/search and deliver puzzle API/play; then shared sharing and generated cards, remaining reliability findings and content. Owner decisions and external launch gates still apply.

## Earlier integration handoff (historical)

This records the resumed integration pass after Claude's app-layout edit failed. It is not a completion claim for all six growth plans. Existing source changes and staged removal of generated build files and the unused Astro site were preserved. Nothing was committed or deployed in this pass.

## Completed in this pass

- Connected the shared theme restoration, first-game hint tracking, notices and account-saving sheet to the app layout.
- Kept socket lifetime tied to user identity, so same-user credential refresh and account upgrade preserve the connection.
- Added testable authentication bootstrap for automatic guest entry on a valid invitation. Dead links create no account; network failures preserve existing credentials and offer retry. Obsolete guest responses cannot replace a newer identity or navigation.
- Added the missing `/invite/[code]` app route and moved `/join/[code]` to server-rendered marketing HTML. Public previews describe the room and its stakes, are uncached and noindexed, and hand human browsers to the app. They currently use the existing static site image; personalized invite images remain in plan 06.
- Fixed invite preflight cancellation, loading-state interference with replay requests, and the joined notice being lost when a command delivers its snapshot before its acknowledgement.
- Mounted account saving above screen changes with a native modal dialog, keyboard focus containment and protection against applying a late upgrade response to another identity.

## Verification

The combined suite passes: 226 tests across 19 files. The production build passes. Existing accessibility/CSS warnings elsewhere are still present. Visual acceptance remains with the owner; no browser gameplay automation or production deployment was performed.

Manual checks:

1. From a fresh browser, scan Play Someone Nearby or open its shared link. A valid free invitation should create a guest and enter the game; an expired link should explain that it is closed.
2. Start a bot game, finish it and open Save account. The sheet should survive result expiry, be keyboard usable, and leave the same game/socket context after successful upgrade.
3. Choose a Profile appearance theme, reload, and confirm it is restored. Entering a first game should dismiss the initial welcome hint.
4. Open an invite while offline, or navigate away while it is loading. Retry should recover without an unexpected late join or identity switch.

## Remaining work after the initial integration pass (historical)

The planning documents remain the roadmap. This integration pass does not finish the dynamic sitemap/public discovery pages, puzzle API and playable puzzle pages, puzzle-generation validation and publishing buffer, bot search workers, personalized share images, or distribution work. The existing article drafts and generator were preserved; successful compilation is not editorial fact-checking or puzzle correctness verification. Decisions still marked unresolved in the plans are not approvals.

## Documentation-to-code review — 10 September 2026 (before the work above)

**Overall status: incomplete.** The table and discrepancies below record the pre-implementation audit; the latest section above records subsequent closures. The completed work above is one integration pass, not completion of Claude's six-plan roadmap or the earlier player-experience audit. The test/build results above are from that pass; this follow-up is source and documentation inspection, without rerunning tests or checking production.

| Plan | Present in the working tree | Still required |
| --- | --- | --- |
| 01 — Technical SEO | Shared JSON-LD component, static brand images/icons, head/robots/language changes, error pages, replay move list and player/replay breadcrumbs | Dynamic sitemap route, caching/filtering/tests, public `/leaderboard` and `/games` pages and navigation. Replace replay eligibility heuristics with shared server policy. Production crawler/card validation remains unverified. |
| 02 — Strategy hub | mdsvex, content index/frontmatter validation, ArticleLayout, interactive DiagramBoard, hub, real `/blog` redirect, four article files | Eight remaining planned articles; editorial and diagram correctness verification; article sitemap entries. Spanish is intentionally deferred by the plan. |
| 03 — Daily puzzle | Prisma models/migration, search wrapper, generator and notation helper | Search/generator correctness tests and deterministic dry run, `puzzles:generate` package command, verified 30-day buffer, puzzle API and attempt handling, SSR pages, playable board, streaks/rewards/sharing, navigation and sitemap integration. No puzzle routes or puzzle test files currently exist. A generator file alone does not establish a working puzzle feature. |
| 04 / 04a — First session | Bot-first lobby, hints, nearby room/auto-ready flow, search fallback, result sheet, same-user account upgrade, guest lifetime, capped bot rewards, analytics helper, SSR invite entry | Generated invite card explicitly required by 04a's owner follow-up; manual mobile/scan/result acceptance and analytics verification. Auto-friend and personal friend-code proposals are not implemented and remain recommendations in the document. |
| 05 — Distribution | README, venue research/drafts/calendar and links-log template | Launch gates, final claim/venue verification, README screenshot, licence decision, actual outreach/posting and measured follow-up. Log entries remain `planned`; no outreach was performed by this pass. |
| 06 — Share loop | Persisted replay ID/status and corresponding socket tests; waiting-room native share/clipboard implementation; marketing image override hook | Shared `src/lib/share.js`, result/replay/profile share surfaces, `/og` rendering/cache/routes/tests for game/player/puzzle/invite cards, profile privacy setting, milestone prompts and their analytics. No OG service or `profilePublic` field exists. |
| Overview — bot concurrency | Synchronous bot search remains in `server/services/botPlayer.js` | Worker pool with bounded search time and lifecycle/concurrency verification, before launch traffic. |

### Confirmed integration discrepancies

1. **Replay eligibility still infers identity from display names.** `src/routes/(marketing)/game/[id]/+page.server.js` maintains guest-name word lists and a `Bot ` prefix check. The API already supplies `isBotGame`, but not participant guest flags. Real account flags and one shared public-game policy must drive the replay page, public games list and sitemap. The plan itself also conflicts: its overview says to noindex bot games, while its detailed sitemap criterion allows a game with one registered human. Resolve this explicitly in the shared policy rather than implementing separate interpretations.
2. **Square numbering disagrees across features.** The replay page numbers row 0 as squares 1–4. `src/lib/content/position.js` and `src/lib/puzzle/notation.js` number from the opposite end so the first mover starts on 1–12. Consolidate notation into a shared utility with representative opening and capture checks; the replay and article descriptions currently cannot both be correct for the same move.
3. **The static sitemap omits all four article URLs.** `src/static/sitemap.xml` lists only home, Spanish home, FAQ, changelog and the strategy hub. Neither `server/routes/sitemap.js` nor an equivalent dynamic route is present.
4. **The invite preview requirement was deferred, not fulfilled.** The SSR `/join/[code]` page still uses the generic site image. 04a explicitly requires the generated host-specific `/og/invite/CODE.png` card to accompany that page.

### Earlier documentation also remains relevant

`docs/player-experience-audit.md` is a findings inventory, not a closed checklist. Its replay-ID statement is stale: the persisted ID is now implemented and tested. Conversely, the chat finding is still supported by source: RoomChat inserts a local message and emits `chat:send` without an acknowledgement, while the server handler has no delivery callback. The other old findings need individual revalidation before being marked resolved. Account/shop/treasury workflows were explicitly outside that audit's complete coverage.

`docs/animation-contract.md` explicitly leaves screen entrances and room-status highlights proposed; implemented board animations do not imply that every animation in its matrix has shipped.

### Continuation order

1. Close the shared public-game eligibility and notation discrepancies, then finish plan 01's sitemap and discovery pages.
2. Validate the puzzle search/generator before exposing puzzle pages or claiming a publishing buffer.
3. Finish puzzle API/play and the shared sharing/image service, including the required invite card.
4. Move live bot search off the socket event loop; revalidate unresolved player-experience findings.
5. Complete the remaining content and manual acceptance checks; evaluate distribution gates separately. Publishing, outreach and production verification are distinct from local code completion.
